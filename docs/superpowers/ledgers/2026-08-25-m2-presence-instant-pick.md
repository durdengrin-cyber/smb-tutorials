# SDD ledger — plan: docs/superpowers/plans/2026-08-25-m2-presence-instant-pick.md

Spec: docs/superpowers/specs/2026-08-25-m2-presence-instant-pick-design.md (read; binding authority)
Branch: m2-presence-instant-pick (off main @ b152570). Baseline: 19 tests passing.

## Pre-flight scan

### Cross-task rows (tasks sharing a file or interface)

| Tasks | Produces → Consumes | Finding |
|---|---|---|
| T1 → T3 | `SessionStatus` union → SQL `status` CHECK list | Both list pending/accepted/active/completed/declined/timed_out/cancelled. Agree. |
| T1 → T7,T8,T9,T10 | `effectiveStatus`, `canTransition`, `secondsRemaining` | All call sites select `status, accept_deadline, started_at, duration_minutes` — the exact fields `effectiveStatus` reads. Agree. |
| T1 → T3 | `SESSION_DURATION_MINUTES=60` vs SQL `duration_minutes default 60` | Constant duplicated in TS and SQL. Intentional (DB default mirrors code); no drift risk within M2. OK. |
| T2 → T5 | `PRESENCE_CHANNEL`, track payload | T5 tracks `{teacher_id, full_name, hourly_rate}` = `OnlineTeacher`. Agree. |
| T2 → T6 | `rosterFromPresenceState`, `intersectOnline<T extends {id:string}>` | T6 passes `TeacherCardData[]` which has `id`. Agree. |
| T3 → T7,T8,T9,T10 | `sessions` columns | Every select names only columns the migration creates. Agree. |
| T4 → T7 | `createSessionRoom(sessionId, apiKey, fetchImpl?, ttl?)` | T7 calls with (sessionId, env key). Agree. |
| T4 → T9 | `createMeetingToken(...)`, `roomNameForSession` | T9 calls with (roomName, userName, isOwner, key). Agree. |
| T5 → T7 | `dashboard/page.tsx` | T7 adds `<IncomingRequest>` to it. Sequential edit, no conflict. |
| T5 → T10 | `dashboard/page.tsx` | T10 adds `<SessionHistory>` to it. Sequential edit, no conflict. |
| **T6 → T7** | **T6 imports `requestSession` from `./actions`, created in T7 Step 1** | **CONFLICT — T6's `npm run build` step fails: the module does not exist yet.** See Ruling R2. |
| T6 → T8 | `redirect("/waiting/{id}")` | T8 creates that route. T6 build unaffected (runtime path). OK. |
| T7 → T9 | Accept redirects to `/call/{id}` | T9 creates it. T7's own verify step already says the 404 is expected. OK. |
| T8 → T9 | `completeSession` in `session/actions.ts` | T8 creates it, T9 imports it. Order correct. |
| T9 → T11 | `call/[sessionId]/page.tsx` vs deleted `call/page.tsx` | Distinct files; Next routes `/call` and `/call/[id]` independently. No conflict. |
| T11 → T4 | Deletes spike, keeps `getOrCreateRoom` | `daily.ts` and its M1 tests survive; only share.ts/route/page go. Agree. |

### Per-task self-consistency rows

| Task | Tests vs code it specifies | Finding |
|---|---|---|
| T1 | 5 describes vs 4 exports + 2 constants | Every asserted behaviour exists in the implementation shown. Agree. |
| T2 | dedupe + malformed-entry tests | `isOnlineTeacher` guard and `seen` set cover both. Agree. |
| T3 | verify SQL vs DDL | Checks table, 3 policies, publication row, CHECK rejection — all created by the DDL above it. Agree. |
| T4 | arg order in tests vs signatures | Match exactly, including optional `fetchImpl` in 3rd/5th position. Agree. |
| T5 | page selects `hourly_rate` from `profiles` | Column exists (M1 migration 0001). Agree. |
| T6 | props passed vs props declared | `page.tsx` already defines `curriculum/grade/stream/subject` via its `one()` helper. Agree. |
| T7 | `redirect()` outside try/catch | Correct — `redirect` throws by design; the room call is the only thing wrapped. Agree. |
| T8 | `accept_deadline!` assertion | Only reached when status is `pending`, which always has a deadline. Agree. |
| T9 | fields used vs fields selected | `started_at`, `daily_room_url`, `duration_minutes` all selected. Agree. |
| T10 | earnings maths | `rate * duration/60`, completed only, ₹0 when empty — matches the no-fabricated-data constraint. Agree. |
| T11 | grep guard before deletion | Names exactly the symbols the deleted files export. Agree. |
| T12 | verification vs what tasks built | Covers every screen and both security checks. Agree. |

## Rulings

Ruling R1: Implement on branch `m2-presence-instant-pick` in the main checkout rather than a git worktree — the EnterWorktree tool's contract forbids use unless the user or CLAUDE.md says "worktree" (neither does), and a fresh worktree would lack the gitignored `.env.local`, breaking every task that verifies against the live Supabase project. `main` and production stay untouched either way. Cost if wrong: the user's checkout sits on a feature branch instead of main; `git checkout main` undoes it.

Ruling R2: Task 6 also creates `src/app/teachers/actions.ts` containing `requestSession` exactly as written in Task 7 Step 1; Task 7 then covers only `acceptSession`/`declineSession` and the prompt. The plan's own note ("build that action first, or stub the import last") is too vague to hand an implementer, and as written Task 6's build step fails on a missing module. Spec is silent on task ordering, so this is a plan defect, not a spec conflict. Cost if wrong: `requestSession` lands one task earlier than planned — no behavioural difference.

Ruling R3: Task 8's verification will see `/call/{id}` 404 until Task 9 lands. Not a defect; Task 7 already documents the same expectation. Cost if wrong: none.

## Progress
Task 1: implemented (commit 7772be5, 10 new tests, 29 total passing) — review dispatched
Task 1: complete (commits b152570..7772be5, review clean — spec ✅, quality approved)
Task 1: minor (deferred): unused `iso` helper in session.test.ts:6 (dead code, inherited from the plan's own snippet)
Task 1: minor (deferred): no test at the exact boundary instant (deadline === now); inclusive comparisons inferred from 1s-before/after cases
Task 1: minor (deferred): secondsRemaining Math.ceil fractional round-up untested
Task 1: Ruling R4: reviewer flagged that effectiveStatus only expires a `pending` row when accept_deadline is non-null, relying on an unstated DB invariant. Resolved forward, not by reopening Task 1: Task 3's migration must add `constraint pending_has_deadline check (status <> 'pending' or accept_deadline is not null)`. Without it a null-deadline pending row never times out and could be accepted arbitrarily later — the exact failure the spec's read-time rule exists to prevent. Cost if wrong: one extra CHECK constraint; harmless if the invariant already held.
Task 2: implemented (commit cac7350, 6 new tests, 35 total passing) — review dispatched
Note: plan + task-3-brief updated with R4's pending_has_deadline constraint before Task 3 dispatch (plan commit pending; will ride with Task 3).
Task 2: complete (commits 7772be5..cac7350, review clean — spec ✅, quality approved)
Task 2: minor (deferred): rosterFromPresenceState throws if a presence VALUE is a non-array truthy object (entry-level malformation is guarded, array-level is not). Low risk — Supabase presenceState() always returns arrays.
Task 2: minor (deferred): isOnlineTeacher validates only teacher_id is a string; full_name/hourly_rate types unchecked. Copied verbatim from the plan's reference guard.
Task 3: Ruling R5: the implementer writes and commits supabase/migrations/0002_sessions.sql but does NOT apply it — applying DDL to the live project needs the Supabase SQL Editor (no DB password here; PostgREST cannot run DDL; Supabase CLI is unauthenticated). Application is controller-owned and batched to one user action. Consequence: runtime verification for Tasks 5-10 is deferred until it is applied; build/type/test verification proceeds normally. Cost if wrong: runtime bugs surface later than they would have, in one batch rather than per task.
Task 3: implemented (commit dcbea79, migration authored; CHECK lists cross-checked against session.ts + taxonomy.ts) — review dispatched; APPLY TO LIVE DB STILL PENDING (see R5)
Task 3: complete (commits ad35295..dcbea79, review clean — spec ✅, quality approved, zero findings)
Task 3: Ruling R6: reviewer's two ⚠️ items (does `supabase_realtime` publication exist; is gen_random_uuid available) are resolved at apply time rather than pre-checked — both are Supabase defaults, and if either is absent the migration fails loudly on the first statement rather than silently. Cost if wrong: one failed paste in the SQL Editor, immediately visible.
Task 4: implemented (commit 1247da3, 4 new tests, 39 total passing) — review dispatched
Task 4: complete (commits dcbea79..1247da3, review clean — spec ✅, quality approved)
Task 4: minor (deferred): new Daily tests never assert the Authorization header — a dropped/malformed auth header would pass silently. Inherited pattern from the pre-existing getOrCreateRoom tests.
Task 4: minor (deferred): only is_owner=false (student) is exercised; teacher path untested until Tasks 7/9 wire real callers.
Task 4: minor (deferred): exp asserted only via toBeGreaterThan(now), which would also pass if exp were computed in milliseconds. Correctness confirmed by inspection.
Task 5: implemented (commit 7a34839, build + tsc clean, 39 tests passing) — review dispatched
Task 5: review — spec ✅, quality Changes-requested. 2 Important: (a) unguarded SUBSCRIBED ack can track() on an already-unsubscribed channel → untrackable ghost teacher (StrictMode reproduces); (b) CHANNEL_ERROR/TIMED_OUT/CLOSED never clear busy → toggle stuck disabled forever. Fix round 1 dispatched to original implementer (FIX_BASE 7a34839).
Task 5: minor (deferred): misleading eslint-disable comment claiming goOnline is stable (it is not memoized).
Task 5: minor (deferred): tracked payload literal is not typed as OnlineTeacher, so the link to presence.ts is convention-only, not compiler-enforced.
Task 5: fix round 1/5 applied (commit c069283 — mounted + current-channel guards; error/CLOSED/TIMED_OUT branch clears busy with inline message; closingRef distinguishes intentional close). First re-review agent stalled on a watchdog timeout (no verdict); re-dispatched.
Task 5: Ruling R7: re-review performed by the controller in-session, not by a dispatched reviewer — two consecutive re-review agents died to infrastructure (watchdog stall, then "computer went to sleep mid-response"). Verified against src/app/dashboard/availability-toggle.tsx: FINDING 1 ADDRESSED (line 72 guards mounted+current-channel before track/setState; line 82 re-checks after the await), FINDING 2 ADDRESSED (lines 90-103 clear busy, stay offline, show inline text-red-600 error; closingRef at 97 suppresses only the intentional-close CLOSED ack). New breakage: none Critical/Important. Cost if wrong: this one fix diff got a less independent review than the others; the final whole-branch review still covers it.
Task 5: minor (deferred): goOffline has no try/finally — if untrack()/unsubscribe() ever rejects, closingRef stays true (later genuine failures silently swallowed) and busy stays true (button stuck disabled). Same failure class as FINDING 2, on the offline path. Narrow: these Supabase calls resolve rather than throw.
Task 5: complete (commits 1247da3..c069283, 1 fix round, review clean)
Task 3: MIGRATION APPLIED to the live project (2026-08-26, user-run in the Supabase SQL Editor). Verified there: table exists (count 0), exactly 3 policies on public.sessions, 1 row in pg_publication_tables for supabase_realtime, and both negative inserts rejected (invalid curriculum 'IB'; pending row with null accept_deadline — R4's constraint fires). **Ruling R5 is now lifted** — runtime verification for Tasks 5-10 is unblocked.
Task 3: plan defect fixed in place — Task 3 Step 3 said `select polname from pg_policies`, but `polname` is a column of the `pg_policy` catalog table, not the `pg_policies` view; it errors. Corrected to `select policyname ... where schemaname = 'public'`.
Task 6: Ruling R8: the plan's OnlineList calls `channel.subscribe()` with no status callback, so a failed handshake leaves roster empty and renders the "No teachers online 🌙" empty state — indistinguishable from genuinely nobody being available, and a direct repeat of the Important finding the reviewer raised against Task 5 (FINDING 2). Implemented with a status callback that sets a separate `connFailed` state and its own "Couldn't check who's online" panel, plus a `mounted` flag guarding both the presence-sync and subscribe callbacks (same StrictMode stale-ack class as Task 5 FINDING 1). Deviation from the plan text, consistent with CLAUDE.md's "airtight, never makeshift" rule. Cost if wrong: one extra state variable and one extra empty-state branch.
Task 6: Ruling R9: per Ruling R2 this task also created `src/app/teachers/actions.ts` with `requestSession` verbatim from Task 7 Step 1. Task 7 now covers only acceptSession/declineSession/incoming-request.
Task 6: implemented — build clean, tsc clean, eslint clean, 39 tests passing (no new tests: Task 6 is all UI wiring over already-tested pure helpers from Task 2). Dev-server smoke test: /teachers?curriculum=CBSE&grade=11th&stream=Science&subject=Physics returns 200 and renders the empty-roster state (correct — SSR has no presence).
Task 6: Ruling R10: NO independent reviewer was dispatched for this task — the session's operating instructions forbid spawning subagents unless the user asks. Reviewed by the controller in-session instead, same posture as Ruling R7. The final whole-branch review (Task 12) still covers this diff. Cost if wrong: this task's diff got a less independent review than Tasks 1-4.
Task 6: PENDING USER VERIFICATION (plan Step 4, two-browser): teacher dashboard toggled Available in one browser must make their card appear in the student's /teachers list within a second or two, and toggling offline must remove it live without a refresh.
Task 6: self-review (per R10) — no Critical/Important findings. Checked: cleanup sets `mounted = false` before `channel.unsubscribe()`, so the CLOSED ack from intentional teardown is suppressed without needing Task 5's closingRef; `requestSession` returns `{error}` only on failure (redirect throws, so `result` is undefined on success) and the call site handles that correctly; presence payload's full_name/hourly_rate are deliberately unused — the DB row stays authoritative for display, presence only answers "who is here".
Task 6: minor (deferred): a teacher with a null hourly_rate can appear in `eligible` (TeacherCard renders "₹—") and is startable, but requestSession rejects them with "That teacher is unavailable." Honest failure, not a crash, but the card should not offer Start. Root fix belongs in the page query (require hourly_rate) rather than in the card.
Task 6: minor (deferred): `connFailed` clears only when a later SUBSCRIBED arrives. Supabase auto-rejoins, so this self-heals, but a permanently dead socket leaves the panel up until refresh — which is what the panel tells the user to do.
Task 6: minor (deferred): no automated tests for the wiring itself; the pure helpers it composes (rosterFromPresenceState, intersectOnline) are covered by Task 2's 6 tests. Matches the plan, which specifies no tests for this task.
Task 6: repo hygiene — added `tsconfig.tsbuildinfo` to .gitignore (untracked tsc incremental cache, was showing up as a stray untracked file). `next-env.d.ts` is left uncommitted: it flips between `.next/types/` and `.next/dev/types/` depending on whether `next build` or `next dev` ran last, and the committed variant is the build one Vercel produces.
Task 7: Ruling R11 (plan defect, Important): the plan's acceptSession guards its update with `.eq("status","pending")` and comments "lost race → 0 rows, student already cancelled" — then redirects to /call/{id} regardless of whether a row was hit. A teacher whose student cancelled between the read and the write would land in a call for a non-active session, with a Daily room already minted. Implemented with `.select("id")` on the update and an explicit 0-row branch returning "That request is no longer waiting." Spec is silent, so this is a plan defect, not a spec conflict. Cost if wrong: one extra round-trip on the accept path.
Task 7: Ruling R12: the plan's Accept/Decline handlers discard the action's `{error}` return (`await acceptSession(id); setBusy(false);`), so every server-side refusal — expired request, already in a session, Daily failure, and now R11's lost race — is silently swallowed and the teacher sees the prompt sit there doing nothing. Added an `error` state rendered above the buttons, same pattern as availability-toggle.tsx. Cost if wrong: one extra state variable.
Task 7: Ruling R13: the plan subscribes to INSERT only, so a request that landed before the component subscribed is never shown — the teacher sits idle while the student's 30s expires. Reachable on a return from a call, a reload, or a socket rejoin. Added a catch-up query on mount (newest pending row for this teacher, discarded if already past its deadline) that uses `setRequest(prev => prev ?? pending)` so a live INSERT racing the query still wins. Cost if wrong: one extra indexed query per dashboard mount — covered by sessions_teacher_status_idx.
Task 7: implemented — build clean, tsc clean, eslint clean, 39 tests passing. Dev smoke: /dashboard 307 -> /signin when anonymous (auth gate intact).
Task 7: Ruling R10 continues to apply — no independent reviewer dispatched (session instructions forbid spawning subagents); reviewed by the controller in-session.
Task 7: minor (deferred): the "already in a session" check counts active rows then writes non-atomically, so two accepts landing in the same instant across two different sessions could both pass. Narrow (one teacher, two students, sub-millisecond window) and the spec puts the state machine in the actions rather than in DB constraints. Proper fix is a partial unique index on (teacher_id) where status = 'active'.
Task 7: minor (deferred): declineSession does not check effectiveStatus, so declining an already-expired-but-still-pending row writes `declined`. Harmless — arguably more accurate than leaving it to time out.
Task 7: minor (deferred): the countdown ticks at 250ms to render a whole-second value. Inherited from the plan; 4x more renders than needed, no correctness impact.
Task 7: PENDING USER VERIFICATION (plan Step 5, two-browser): teacher available on /dashboard, student clicks "Start now →" -> prompt appears with live countdown; Accept -> teacher hits /call/{id} (404 until Task 9, expected) and the row reads `active` with a daily_room_url; Decline -> `declined`; ignoring it 30s leaves the row `pending` but hides the prompt, and a later Accept is refused with "That request has already expired."
Task 8: Ruling R14 (plan defect): the plan's Step 4 asserts "Cancel sets `cancelled` and the teacher's prompt disappears", but IncomingRequest (Task 7) subscribes to INSERT only — a cancelled or expired row leaves the prompt on screen offering an Accept that can only fail. Added a second postgres_changes listener on UPDATE (filter teacher_id=eq.{id}) that clears the prompt when the row leaves `pending`, matched by id via functional setState so the callback needs no dependency on current state. Without it the plan's own acceptance criterion for this task cannot pass. Cost if wrong: one extra listener on an already-open channel.
Task 8: Ruling R15: on countdown expiry the plan pushes the student to /teachers?didNotRespond=... unconditionally. If the realtime socket dropped, the teacher may have accepted at second 25 — the row reads `active`, and the student would be sent away from a session the teacher is now sitting in alone. Replaced the blind push with router.refresh(): the server page re-reads the row and applies effectiveStatus, sending the student to /call/{id}, to /teachers, or nowhere. This makes the server the single authority on expiry, which is the spec's read-time rule rather than a new mechanism. Cost if wrong: one RSC round-trip at t=0 instead of a client-side push.
Task 8: also added a `leavingRef` guard — router.push/refresh is not instant, and the 250ms interval otherwise keeps firing between the decision to leave and the unmount, repeating the navigation several times. Same guard suppresses the realtime callback once a navigation is already committed.
Task 8: implemented — build clean, tsc clean, eslint clean, 39 tests passing. /waiting/[sessionId] registered in the route table; anonymous GET returns 307 -> /signin (auth gate intact).
Task 8: note — the first `npx tsc --noEmit` after creating the route failed with "Type '/waiting/[sessionId]' does not satisfy the constraint 'AppRoutes'". Not a code defect: Next generates the typed-routes table during build, so a brand-new route needs one `npm run build` before standalone tsc knows it. Clean afterwards.
Task 8: Ruling R10 continues to apply — no independent reviewer dispatched; reviewed by the controller in-session.
Task 8: minor (deferred): WaitingClient does not handle subscribe() status. A dead socket is now survivable rather than silent (R15 makes expiry re-ask the server), so this degrades to "the student waits the full 30s instead of moving the instant the teacher accepts" rather than losing the session.
Task 8: minor (deferred): cancelSession/completeSession return void and swallow the update error, so a failed cancel still navigates the student to /teachers while the row stays `pending` — the teacher could then accept a session the student believes they cancelled. Narrow (the guards make 0 rows the normal outcome, indistinguishable from an error under a void return). Proper fix is returning {error} like the dashboard actions; deferred because Task 9 consumes completeSession and the signature change belongs in one place.
Task 8: PENDING USER VERIFICATION (plan Step 4, two-browser): "Start now →" lands on the waiting screen with a live countdown; teacher Accept moves the student to /call/{id}; Decline and 30s-of-silence both return the student to /teachers with the "{teacher} didn't respond" banner; Cancel sets `cancelled` AND clears the teacher's prompt (R14).
Task 9: Ruling R16: the plan's call frame keeps the M0 spike's `if (DailyIframe.getCallInstance() || !wrapRef.current) return;` guard, which bails silently when an instance already exists — leaving a blank pane, no error, and no call. Acceptable in a throwaway spike; not on the product's core screen, where StrictMode remounts and back-navigation both reach it. Replaced with `const stale = DailyIframe.getCallInstance(); if (stale) await stale.destroy();` so the mount actually gets a call. Cost if wrong: one extra teardown on a path that previously did nothing.
Task 9: Ruling R17: createMeetingToken throws (daily.ts raises on a non-OK response), and the plan's page calls it unguarded — a Daily outage would render Next's error page to a student mid-session. Wrapped in try/catch returning a plain "Couldn't open the call" panel with a Back link. redirect() deliberately stays outside the try, since it works by throwing. Cost if wrong: one extra branch in the page.
Task 9: also: `left` is seeded via lazy initial state instead of 0 (the plan paints 00:00 for one frame before the first tick, which on a 60-minute call reads as "already over"), and an `endingRef` makes the two end paths — the Daily leave button and the countdown hitting zero — idempotent, so completeSession + router.push cannot fire twice.
Task 9: closes the Task 8 minor about completeSession's void return: effectiveStatus() already reports an `active` row past started_at + duration_minutes as `completed`, so a failed write self-heals at read time by design. No signature change needed. The cancelSession half of that minor stands but is bounded by the 30s accept window.
Task 9: implemented — build clean (route table now lists /call/[sessionId] and /waiting/[sessionId]), tsc clean, eslint clean, 39 tests passing. Anonymous GET /call/{uuid} returns 307 -> /signin.
Task 9: Ruling R10 continues to apply — no independent reviewer dispatched; reviewed by the controller in-session.
Task 9: minor (deferred): the countdown trusts the client clock (Date.now()) for auto-end. Server-side effectiveStatus is the real authority on every read, so skew shows a wrong number rather than granting extra time.
Task 9: minor (deferred): no automated tests — the screen is Daily SDK + DOM wiring over already-tested helpers (createMeetingToken/roomNameForSession have 4 tests from Task 4, effectiveStatus 10 from Task 1). Matches the plan, which specifies none.
Task 9: PENDING USER VERIFICATION (plan Step 3, two-browser, DAILY_API_KEY required): accept lands both parties in the call, each sees the other's name + subject, countdown runs from 60:00, video/audio/screen-share work; leaving marks `completed` and returns each to their own page; re-visiting /call/{id} after completion redirects away; a third signed-in user visiting the URL is redirected to /.
Task 10: Ruling R18 (plan defect): the plan formats timestamps with `new Date(r.created_at).toLocaleString("en-IN")` inside a Server Component. The locale argument sets the format, not the zone — the zone comes from the runtime, which is UTC on Vercel. Every session in an Indian teacher's history would read 5:30 off. Fixed with an explicit `{ timeZone: "Asia/Kolkata", dateStyle: "medium", timeStyle: "short" }`, which is also deterministic regardless of where the function runs. Cost if wrong: times are pinned to IST rather than the viewer's zone — correct for this product's audience, and revisit only if it serves other regions.
Task 10: implemented — build clean, tsc clean, eslint clean, 39 tests passing. Earnings use effectiveStatus, so a row still stored `active` past its hour counts as completed, matching the read-time rule rather than the column.
Task 10: Ruling R10 continues to apply — no independent reviewer dispatched; reviewed by the controller in-session.
Task 10: minor (deferred): the table shows raw status enum values (`timed_out`, `declined`) rather than prose. Honest and unambiguous, just unpolished; specified this way by the plan.
Task 10: minor (deferred): `.limit(25)` with no "showing latest 25" affordance — a busy teacher silently sees a truncated history. Fine at M2 volumes.
Task 10: PENDING USER VERIFICATION (plan Step 3): after one completed session the dashboard lists it as `completed` and earnings read ₹500 for a ₹500/hr 60-minute session; a brand-new teacher sees ₹0 and the empty message, never a fabricated figure.
Task 11: grep guard clean — every match for `api/rooms|lib/share|buildShareUrl` was inside the four files being deleted. Also checked for links to the old `/call` route: the only remaining references are `/call/${sessionId}` (the new dynamic route) in waiting-client, waiting/page and dashboard/actions.
Task 11: deleted src/app/call/page.tsx, src/app/api/rooms/route.ts, src/lib/share.ts, src/lib/share.test.ts. src/app/api/ is now empty and gone. Verified on the dev server: /call -> 404, /api/rooms -> 404, /call/{uuid} -> 307 (auth gate). Tests 39 -> 38 (the one share test went with its module); build clean; route table no longer lists /call or /api/rooms.
Task 11: spec §15 rewritten — header now reads "Closed in M2", and each of the four original debts carries how it was closed rather than what is still open.
Task 11: Ruling R19: `getOrCreateRoom` stays, as the plan directs, but after this deletion it has ZERO callers — only its own 4 tests. It creates a *public* room from an arbitrary name, which is precisely the model §15 just closed, so a future caller reaching for it would silently reopen the hole. Not deleted unilaterally because the plan explicitly names it as staying and its future use is the user's call. Recorded in spec §15 as "carried forward, not a gate": delete it, or give it a private-by-default, when something next needs a room. Cost if wrong: dead code with green tests sits in daily.ts, and the trap is written down rather than latent.
Task 11: also closed the Task 1 deferred minor — removed the unused `iso` helper from session.test.ts:7. eslint across src/ is now warning-free.
Task 11: Ruling R10 continues to apply — no independent reviewer dispatched; reviewed by the controller in-session.
Whole-branch review DISPATCHED (user-authorised, lifting Ruling R10 for this pass): general-purpose reviewer over c069283..f10e4cf (Tasks 6-11, 21 files, +1027/-163). Brief names all nine deviations (R8, R11-R18) and asks the reviewer to independently verify each defect was real and each fix is correct, rather than take the ledger's word. Also directed at the accept-path race, what RLS actually permits vs what the Server Actions assume, Daily token minting / is_owner, realtime subscription lifecycles, client-clock trust in the 60-minute auto-end, and the earnings arithmetic. Reviewer instructed read-only and told not to run `npm run build` (a dev server is live on this checkout).
Test credential (local ledger only — .superpowers/ is gitignored; do NOT write this into project_state.md, which is tracked): teacher account tutor-check@smbtutorials.in / [REDACTED — see the machine-local ledger, or rotate it] — password set 2026-08-26 via the Supabase admin API at the user's request, verified with a real /auth/v1/token?grant_type=password sign-in. Throwaway account, already flagged in project_state.md for deletion before launch.
Verification context established: the live DB holds exactly two profiles — Dr. Rao (teacher, 774d5217…, ₹500/hr) and Tyler (student, fbc550d4…). sessions table is empty, so the two-browser run starts from a clean slate and every row it creates is attributable to the test.
Confirmed NOT a bug: /teachers rendering "No teachers online" with no teacher tab open is correct M2 behaviour. The server payload for that URL does contain Dr. Rao, so the taxonomy query works; only the presence intersection is empty. Pre-M2 the page listed all eligible teachers regardless of presence, which is why this reads as a regression.
WHOLE-BRANCH REVIEW RETURNED — verdict: ready to merge WITH FIXES, explicitly NOT ready to push (branch auto-deploys to production). 2 Critical, 8 Important, 9 Minor. Deviation verdicts: R8, R11, R13, R14, R16, R17, R18 upheld as real defects correctly fixed; R12 partially (right diagnosis, placement makes 2 of 3 messages unrenderable); R15 REJECTED — the fix introduces a worse bug than the one it replaced.
Review CRITICAL 1 (verified in code): acceptSession's concurrent-session check (dashboard/actions.ts:36-41) is the ONLY status read in the codebase that bypasses effectiveStatus. completeSession is client-only, so both tabs closing mid-call leaves status='active' forever -> every future Accept returns "You are already in a session", while session-history shows that same row as completed. Teacher permanently removed from the product, recoverable only by manual DB edit. This is design spec §3.2.1 rule 3's exact scenario: the rule was implemented as a derivation but never as a write. Inherited verbatim from the plan.
Review CRITICAL 2 (verified against migration text + session.ts:54): the sessions UPDATE policy is row-level with no column restriction, so either participant can PATCH any column via PostgREST with the anon key. Student sets status='active' leaving started_at null; effectiveStatus's expiry branch is gated on row.started_at, so it returns "active" forever -> permanent DoS on any teacher, one HTTP call, no recovery path in the product. Also leaves hourly_rate mutable by both parties after the snapshot, defeating the reason §4 snapshots it, with Stripe landing on that column in M3.
Review IMPORTANT 3 (verified empirically with Dr. Rao's token): profiles SELECT policy is `role = 'teacher' or id = auth.uid()`, so a TEACHER reading a STUDENT profile gets []. Confirmed live: teacher reads student profile -> 200 [], own profile -> 200 [{"full_name":"Dr. Rao"}]. Consequence: the incoming-request prompt renders "A student wants ..." and the teacher's in-call bar renders "Your session" — both fallbacks fire silently. Fix preference: snapshot student_name onto the sessions row at insert, matching the hourly_rate snapshot precedent.
Review IMPORTANT 4: R15's leavingRef latch is one-way and survives router.refresh() (same client component instance, refs preserved). If the server still reads pending — a client clock a few hundred ms ahead is enough — the interval and the postgres_changes handler both return early forever and the student is frozen at "0" with a dead subscription. My fix converted a wrong-navigation bug into a stuck-screen bug. Fix: separate refreshingRef with a ~2s cooldown; reserve leavingRef for genuine navigations.
Review IMPORTANT 5: earnings reduce over the SAME .limit(25) rows as the table, so a teacher with 30 completed sessions sees the payout for 25. I logged the limit as a display minor and missed that it silently understates money. Fix: separate aggregate query from the display query.
Review IMPORTANT 6/7/8/9/10 (all accepted): createSessionRoom is not idempotent so a half-failed accept can never be retried (Daily 400s on duplicate room name; the getOrCreateRoom it replaced had GET-first); nothing ever writes timed_out (spec §3.2 enforcement point 1 dropped silently when R15 replaced the navigation); no cap on a student's concurrent requests, so two teachers can both accept and the loser's row later counts toward earnings for a session that never happened; room exp defaults to 7200s = 2x the 60-minute session, the only server-side cap on call length; R12's error banner sits inside `if (!request) return null` so the two most important messages unmount before they render.
Review TESTING verdict: the no-tests position is ~80% defensible, but two pieces of business logic were written inline in IO code instead of src/lib where this project's own convention would have tested them — the busy check (would have caught Critical 1 on the first test) and the earnings reduction (would have caught Important 5). Highest-value single addition: extract hasLiveSession(rows, now) into session.ts and test the started_at: null case.
FIX ROUND (post-review) — Criticals 1 and 2.
Critical 1 FIXED: extracted `hasLiveSession(rows, now)` and `expiredActiveIds(rows, now)` plus `SessionTimingRow` into src/lib/session.ts — exactly where the reviewer said the logic should have lived, and the reason it was never tested. 9 new tests (38 -> 47), including the two cases that would have caught this on day one: an active row past its hour must NOT count as live, and an active row with a null started_at must NOT count as live. acceptSession now selects the timing columns, decides with hasLiveSession, and writes `completed` back to expiredActiveIds — the read-time rule now settles rather than only deriving.
Design note: expiredActiveIds deliberately skips rows with a null started_at. Settling one to `completed` would fold a forged row into the teacher's earnings, which is the exact fabricated-figure outcome the spec forbids. Those rows are handled at the source by migration 0003 instead.
Critical 2 VERIFIED EMPIRICALLY, then fixed. Wrote a probe (job tmp dir, probe-session-rls.mjs) that seeds one throwaway session with the service role, attacks it with Dr. Rao's own JWT straight through PostgREST, then deletes it. Result BEFORE the fix — all five permitted, HTTP 200: rewrite hourly_rate 500 -> 99999 · jump pending -> completed skipping the call entirely · forge active with no started_at (the permanent-lockout row) · rewrite subject after the fact · move accept_deadline an hour out. Two of these are worse than the review stated: pending -> completed in one PATCH fabricates a completed session that then counts toward earnings, and subject/accept_deadline were not listed as mutable at all. Table returned to 0 rows.
Critical 2 FIXED (pending apply): supabase/migrations/0003_session_integrity.sql — before-insert and before-update triggers. Insert: must start pending, cannot arrive already in a call, hourly_rate must equal the teacher's profile rate (read inside the function, not trusted from the caller), accept_deadline capped at now()+60s. Update: eleven columns declared immutable; transition table mirrors ALLOWED in session.ts; only the teacher may answer, only the student may cancel, only a participant may complete; timed_out only after the deadline; an active row must record started_at. Triggers rather than column grants, so the Server Actions keep writing through the user's token — the rule is about which transition by whom, not which role.
Traced every existing write against the new triggers before committing: requestSession insert, acceptSession (pending->active with started_at + room url), declineSession, cancelSession, completeSession, and the new settle write (active->completed by the teacher, a participant) all remain legal.
Verification after fix round: 47 tests passing, tsc clean, eslint clean, build clean. Migration 0003 NOT YET APPLIED — same controller-owned batching as Ruling R5.
Migration 0003 APPLIED to the live project (2026-08-26, user-run). VERIFIED BOTH DIRECTIONS against the live DB:
  Attack probe re-run — all five previously-permitted writes now refused 400 with specific messages: hourly_rate rewrite and subject/deadline rewrite -> "session terms are immutable"; pending -> completed -> "illegal session transition pending -> completed"; forged active with null started_at -> "an active session must record started_at".
  Regression probe (new, probe-happy-path.mjs) — every write the app actually makes still permitted: requestSession insert, acceptSession pending->active with started_at + room url, completeSession active->completed by a participant, declineSession pending->declined by the teacher, and Critical 1's settle write (expired active -> completed). Three malformed inserts correctly refused even under the service role, since the triggers are not role-gated: inflated hourly_rate, insert already marked active, and a one-hour accept window.
  Both Criticals are now closed and evidenced rather than asserted. sessions table left at 0 rows.
FIX ROUND 2 (post-review) — all eight Important findings.
Important 3 FIXED: migration 0004 adds sessions.student_name and has enforce_session_insert fill it from profiles under security definer, so the value cannot be forged and requestSession needs no change. Chosen over widening the profiles SELECT policy: it matches the hourly_rate snapshot precedent (§4 — history records what was true then) and does not expose every student's profile to every teacher. incoming-request now reads the column and drops the profiles query that returned [] every time; the call page uses the snapshot for the teacher and keeps the profile read for the student (teachers ARE readable); session-history gains the Student column spec §5 asked for.
Important 4 FIXED (my bug from R15): split refreshingRef from leavingRef. router.refresh() re-renders the same client component instance so refs survive it — latching `leaving` there killed the interval AND the subscription permanently, stranding any student whose clock ran a few hundred ms fast. Expiry now throttles at 2s and leaves both live; leavingRef is reserved for genuine navigations (cancel, and the active/terminal branches).
Important 5 FIXED: earnings now run a second query over all pending-or-completed rows rather than reducing the same .limit(25) the table renders. A teacher past 25 sessions was silently shown an understated payout. Footnote appears only when the page is full, and says explicitly that earnings cover everything.
Important 6 FIXED: createSessionRoom falls back to GET /rooms/{name} when POST returns 400/409. Room names derive from the session id, so a retry after a half-failed accept previously 400'd forever and stranded the session. POST-first (not GET-first) keeps the happy path at one round trip and left the existing test passing unchanged. 2 new tests.
Important 7 FIXED: timeOutSession added to session/actions.ts and called on countdown expiry, restoring spec §3.2 enforcement point 1 which R15 had silently dropped. Guarded with .lt("accept_deadline", now) so Postgres, not the client clock, decides whether the window closed — a fast client writes nothing.
Important 8 FIXED: requestSession refuses when the student already has an open request or a live call, decided with the new hasOpenRequest over effectiveStatus. Two live requests could both be accepted, leaving the losing teacher alone for an hour in a row that later counted toward earnings.
Important 9 FIXED: ROOM_GRACE_MINUTES = 15 and roomTtlSeconds() in session.ts; rooms and tokens now expire at the same absolute wall-clock end (session + grace) instead of a flat 7200s. Previously a client with a slow clock could stay in the room for 2h on a 60-minute purchase; the room is now the cap. 3 new tests.
Important 10 FIXED: the error banner is hoisted above `if (!request) return null` and made dismissible. Two of the three messages R12 was meant to surface — "already expired" and "no longer waiting" — are produced by the very events that clear `request`, so they unmounted in the same tick they were set.
Minor also fixed: call-frame's `await stale.destroy()` moved inside the try, where a rejection now reaches setError instead of reproducing R16's blank pane as an unhandled rejection.
Verification: 57 tests passing (47 -> 57, +10 across roomTtlSeconds, hasOpenRequest and the room-retry path), tsc clean, eslint clean, build clean. Migration 0004 NOT YET APPLIED.
Migration 0004 APPLIED to the live project (2026-08-26, user-run) and VERIFIED:
  Both earlier probes re-run — 0003's protections survived the create-or-replace of both trigger functions (all five attacks still refused 400; all five legitimate app writes still permitted; all three malformed inserts still refused).
  New probe (probe-student-name.mjs): an insert claiming student_name "Totally Not The Real Name" came back as "Tyler" — the trigger overwrites the caller's claim from profiles rather than trusting it. A teacher PATCH of student_name is refused with "session terms are immutable". And the teacher can now read the name straight off the session row: [{"student_name":"Tyler","subject":"Physics"}] — the exact read that returned [] against profiles before this migration. sessions table left at 0 rows.
ALL REVIEW FINDINGS NOW CLOSED: 2 Critical, 8 Important, plus the call-frame destroy-inside-try Minor. Remaining review Minors deliberately deferred (see below).
Deferred review Minors, none blocking: room leak logging on the accept failure branches · online-list's "Refresh to try again" copy is unnecessary since phoenix rejoins on its own, plus no grace period before showing the panel · waiting-client has no catch-up read on SUBSCRIBED (the asymmetry with R13's teacher-side fix; bounded now that expiry re-asks the server rather than latching) · only one pending request is ever displayed, a second INSERT overwrites the first · didNotRespond is unvalidated text rendered on /teachers (React escapes it, so content-injection not XSS) · sessions.subject still has no CHECK constraint, though 0003's insert trigger now blocks the forged-insert route to it · session-history still renders raw status enum values.
REMAINING BEFORE TASK 12: (1) user's two-browser end-to-end run — never yet performed; (2) project_state.md still says "next action: write the M2 implementation plan" and lists the deleted /call spike route, which Task 12 Step 4 covers.
Task 12 Step 1 (automated) COMPLETE: 57 tests passing, tsc --noEmit exit 0, tsconfig.tsbuildinfo removed, eslint clean across src/, production build clean (11 routes; /call and /api/rooms absent as expected).
Task 12 Step 3 (security) COMPLETE, verified against the live DB rather than by inspection. Wrote probe-third-party.mjs: seeds a real Tyler<->Dr. Rao session, creates a throwaway third account, then checks isolation and deletes both. Results — third user selects the session by id -> [] · third user lists every session -> [] · anonymous visitor selects the session -> [] · third user tries to decline it -> 204 with 0 rows affected. Cleanup confirmed: sessions 0 rows, profiles back to exactly "Dr. Rao, Tyler". The remaining half of Step 3 (a student hitting /dashboard redirects to /, a third user hitting /call/{someone-elses} redirects to /) is browser-side and rides with the user's two-browser run; both are already enforced in the page code (dashboard redirects on role !== teacher, call page redirects when the viewer is neither participant).
Task 12 Step 4 (project_state.md) COMPLETE: rewritten so a cold session lands on the right next action. Resume block now says M2 is code-complete on an unmerged branch and the next action is the two-browser run, names both test accounts, and states the presence rule up front (open tab = online; an empty /teachers list is almost always nobody having a tab open, not a bug). Deliberately does NOT record the teacher password — that stays in this gitignored ledger. Now block records the three applied migrations, the closed §15, and the review outcome. Routes list corrected (adds /dashboard, /waiting/[sessionId], /call/[sessionId]; drops the deleted spike). Test count 19 -> 57. Build order marks M2 code-complete rather than "next". The four carried-forward review Minors are recorded under "Open before real launch" so they survive into M3.
Task 12 Step 5 PREP: git fetch origin main done. Branch is 15 commits ahead, 0 behind; origin/main is an ancestor of HEAD, so the rebase is a fast-forward and no conflicts are possible. NOT pushed — Steps 2 and 6 (the two-browser runs, local then production) are the user's and gate the deploy.
TWO-BROWSER RUN #1 (user, 2026-08-26) — found three real bugs that every automated check had missed. All three diagnosed to root cause with live probes, not inspection.
BUG A (Critical, silent): postgres_changes never reached the teacher; the request only appeared on a manual refresh, by which point the student's 30s was already running. ROOT CAUSE: postgres_changes are RLS-filtered against the identity on the SOCKET, which is not the identity on REST calls. supabase-js pushes the JWT down via an INITIAL_SESSION auth event (SupabaseClient dist/index.mjs:836-846), but that fires a tick or two after the client is constructed — so a component that creates a client and subscribes in the same effect joins as `anon`. Proved with probe-realtime.mjs: anon-socket subscribe=SUBSCRIBED, INSERT events heard 0; authed-socket subscribe=SUBSCRIBED, INSERT events heard 1. The failure is total and silent — subscribe() still acks SUBSCRIBED. FIX: createRealtimeClient() in lib/supabase/client.ts awaits getSession() and realtime.setAuth() before returning; incoming-request and waiting-client both await it before subscribing. Presence was never affected because presence is not RLS-filtered — which is exactly why the teacher appeared online while requests went nowhere.
BUG B (Critical): Accept always failed with "Couldn't start the call — try again"; every session row showed room·, so no room was ever minted. ROOT CAUSE: `privacy` is a TOP-LEVEL field on a Daily room, not a room property. Nested, Daily returns 400 "invalid property name 'privacy'". Confirmed by calling the real API both ways: nested -> 400; top-level -> 200 privacy=private. This shipped in Task 4 and survived review because daily.test.ts asserted the nested shape against a mock — the test agreed with the bug. FIX: privacy moved to top level; the test now asserts body.privacy === "private" AND body.properties.privacy === undefined. Also verified against the real API that Important 6's retry path works: a duplicate name returns the same room rather than throwing.
BUG C (UX, real): after a failed or declined attempt the student landed on a bare /teachers — unfiltered, "Showing all teachers", and Start now could only return "Pick a subject before starting." ROOT CAUSE: the didNotRespond redirect carried only the teacher name, dropping curriculum/grade/stream/subject. FIX: the waiting page builds both a backToList and a returnTo URL from the session row's own criteria and passes them to WaitingClient, so cancel and every terminal path return the student to their filtered list. Also: online-list now hides the Start button and points at /find when no criteria are present, rather than offering a click that can only fail.
Also fixed (this is what made the round expensive to diagnose): both swallowed catches now log the real cause server-side before returning their user-facing sentence. acceptSession's `catch {}` discarded the Daily 400 entirely, so an entire test round produced a generic "try again" with nothing in the dev log.
Also added on the student's socket: a router.refresh() on the SUBSCRIBED ack, closing the asymmetry the reviewer noted as a Minor (a teacher's accept landing while the socket was still connecting was previously only noticed at countdown expiry).
Verification: 57 tests, tsc clean, eslint clean, build clean; realtime probe and a live createSessionRoom/createMeetingToken round trip both pass against the real services.
TWO-BROWSER RUN #2 (user, 2026-08-26) — SUCCESSFUL. Task 12 Step 2 COMPLETE. Verified against the live sessions table rather than by report: 5 rows, statuses timed_out x2, cancelled, completed, declined — every terminal status the plan's data check names, and ZERO stale pending/active rows. The completed row (29fc0af6) carries a daily_room_url, i.e. a real private room was minted server-side on an authenticated accept, both parties joined, and leaving settled the row. Earnings should read ₹500 (full rate on an early leave, per decision §2.2).
Task 12 Step 3: the DB half was proved earlier with probe-third-party.mjs (non-participant and anonymous reads both [], non-participant update 0 rows). The browser half (a student hitting /dashboard, a third user hitting someone else's /call/{id}) rides on the same page-level guards already verified anonymous -> 307, and is backed by the RLS result; not separately walked in a browser.
REMAINING: Task 12 Step 5 (rebase + push) and Step 6 (re-run the loop against production once Vercel reports Ready).
Note: 5 test session rows now sit in the live DB, all belonging to Dr. Rao / Tyler. They are the evidence for this run; delete them with the test teacher before launch.
Task 12 Step 5 COMPLETE — pushed. Pre-push checks per CLAUDE.md: branch confirmed, fetched origin/main, 19 ahead / 0 behind, origin/main an ancestor of HEAD so the merge was a genuine fast-forward with no rebase and no conflict surface. Sequence: checkout main -> pull --ff-only -> merge --ff-only m2-presence-instant-pick -> push. b152570..790db95, 34 files, +2475/-188. Local main and origin/main now identical.
Task 12 Step 6 COMPLETE — production verified, not merely "Ready". Vercel deployment b7i9b32au reported Ready after a 28s build; then every route curl'd against https://smb-tutorials.vercel.app: / /find /teachers /signin /signup /tutor-signup /terms all 200; /dashboard, /waiting/{uuid}, /call/{uuid} all 307 (auth gate live in production); /call and /api/rooms both 404, confirming the M0 spike is gone from the deployed build as well as the repo. /teachers with real criteria returns live Supabase data (Dr. Rao present in the payload) and correctly renders the "No teachers online" state with no teacher tab open. This is the check project_state.md exists to force: the project once showed Ready while every route 404'd.
M2 IS COMPLETE AND DEPLOYED. 12/12 tasks. 57 tests, tsc clean, eslint clean. Migrations 0002-0004 live (shared Supabase project, so production needed no separate apply).
Still open, carried forward: delete the 5 test session rows and the Dr. Rao account before launch · getOrCreateRoom has no caller and still mints PUBLIC rooms (spec §15 trap) · the seven deferred review Minors · the browser half of Task 12 Step 3 (student -> /dashboard, third party -> someone else's /call) was covered by page guards + the RLS probe rather than walked in a browser.

================================================================================
SESSION CLOSE — 2026-08-26. M2 SHIPPED. This ledger is complete; nothing in M2
is left half-done. A new session should read project_state.md first, not this
file — this is the detailed audit trail, project_state.md is the entry point.
================================================================================

M2 final state: main @ 790db95, deployed to production as Vercel b7i9b32au and
verified route by route against the live URL. 12/12 tasks. 57 tests, tsc clean,
eslint clean. Migrations 0002 (sessions + RLS + realtime), 0003 (integrity
triggers), 0004 (student_name snapshot) applied to the live project and each
verified in both directions — that they block what they should, and that they
permit every write the app actually makes.

What this milestone cost, and where the bugs actually came from — worth reading
before M3, because the pattern repeats:
  * 8 defects were in the PLAN, found while implementing it (R8, R11-R18).
  * 2 Critical + 8 Important were found by the dispatched whole-branch review,
    none of which the tests, types, lint or build caught.
  * 3 MORE were found by the first two-browser run, of which two were invisible
    to every automated layer: a realtime socket that acks SUBSCRIBED and then
    silently delivers nothing, and a Daily payload whose unit test asserted the
    WRONG SHAPE against a mock — so the test agreed with the bug and passed
    through a full code review.
  The lesson for M3: a passing mock-based test is not evidence about a third-
  party API's contract, and a green realtime subscription is not evidence that
  events arrive. Probe the real service.

Carried forward, all recorded in project_state.md:
  * Delete the 5 test session rows and the Dr. Rao account before launch.
  * getOrCreateRoom in daily.ts has NO caller and still mints PUBLIC rooms —
    the single most likely way someone silently reopens spec §15 in M3.
  * 7 deferred review Minors (see the Task 6-11 entries above).
  * The browser half of Task 12 Step 3 was covered by page guards + the RLS
    probe rather than walked in a browser.

Reusable probes written this milestone (job tmp dir is ephemeral — re-create
from this ledger's descriptions if needed; scripts/watch-sessions.mjs IS
committed and is the useful one):
  * probe-session-rls.mjs      — attacks a seeded session with a participant's
                                 own JWT via PostgREST; proves the 0003/0004
                                 triggers hold.
  * probe-happy-path.mjs       — proves those triggers still PERMIT every write
                                 the app makes. Run both together; a migration
                                 that blocks real writes is worse than the bug.
  * probe-third-party.mjs      — creates a throwaway third account, proves
                                 session isolation, deletes it.
  * probe-realtime.mjs         — anon socket vs authed socket, INSERT events
                                 heard. This is the one that found the silent
                                 realtime bug.
  * probe-student-name.mjs     — proves the insert trigger overwrites a forged
                                 student_name and that the column is immutable.

NEW THREAD OPENED AND LEFT UNFINISHED: site redesign. Full state, findings and
the exact open questions are in project_state.md under "Redesign brainstorm —
open". Summary: a signed-in teacher had no route to /dashboard; the real gap is
that no authenticated shell exists anywhere. Classified architectural. User has
chosen FULL VISUAL REDESIGN as the scope. The primary-device question was asked
and withdrawn for clarification — re-ask it first, because it collides with the
presence model rather than being a styling preference. No design approved, no
spec written, no code touched. The brainstorming gate has NOT been passed.

Environment left running: a `next dev` server on :3000 (background). Kill with
`pkill -f "next dev"` if it is in the way.

ADDENDUM (same session, after the close block above): the redesign thread was
scoped further and then DEFERRED.

The user named what was missing: three dashboards — student, teacher, admin —
plus a polish pass on "small things not given attention". Verified against the
schema: profiles.role is check (role in ('student','teacher')), so NO admin
role exists; grep finds zero mention of admin in src/, supabase/ or the specs.
Admin is a missing subsystem, not a missing screen — it needs a migration, its
own RLS policies, and policy decisions. It is also load-bearing for three
existing launch blockers: manual payouts (rendered on the teacher dashboard
today), the no-show/refund policy, and the trust & safety escalation path for
minors, which currently has nowhere to escalate TO.

Per the brainstorming skill's scope rule, this was flagged as needing
decomposition rather than more detail questions. Agreed decomposition, four
separate spec -> plan -> implement cycles: (1) IA + design system, (2) student
dashboard, (3) admin, (4) polish. Rationale for that order is in
project_state.md.

SEQUENCING DECISION: the user chose to ship M3 Stripe Checkout FIRST, on the
current UI, and do all redesign work after. The trade-off was stated in the
option they picked — the redesign then has to absorb the Stripe surfaces, and
admin/payouts stays manual while real money moves. They chose it anyway; it is
recorded, not to be re-litigated. Plan M3 knowing its UI is temporary.

NEXT SESSION STARTS AT: M3 Stripe Checkout. Nothing about M3 has been started.
The `accepted` status is already reserved in src/lib/session.ts's ALLOWED table
for where payment sits between accept and call.
