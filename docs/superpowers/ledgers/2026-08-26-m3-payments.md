# SDD ledger — plan: docs/superpowers/plans/2026-08-26-m3-payments.md

Spec: docs/superpowers/specs/2026-08-26-m3-payments-design.md (read; binding authority)
Branch: m3-payments (off main @ 5f1cc24). Baseline: 57 tests passing, tsc clean, build clean.
Note: the plan and spec were written by this controller in this session, so the pre-flight scan is a genuine adversarial re-read, not a formality.

## Pre-flight scan

### Cross-task rows (tasks sharing a file or interface)

| Tasks | Produces → Consumes | Finding |
|---|---|---|
| T1 → T2 | `ALLOWED` transition table → trigger's `old.status` clauses | Both list pending→{accepted,declined,timed_out,cancelled}, accepted→{paid,payment_expired,cancelled}, paid→{active,refunded}, active→{completed}. Agree. T2 Step 2 re-checks by hand. |
| **T1 → T7** | **`SessionTimingRow` gains `payment_deadline` → 7 queries across 5 files** | **CONFLICT — Task 1 as written leaves `tsc` failing and Task 7 fixes it, so Task 1's commit does not type-check.** See Ruling R1. |
| T1 → T4, T5 | `amountPaiseFor` | Both call it; spec §3.3.1 forbids independent computation. Agree (fixed during plan self-review — the webhook had been re-implementing it inline). |
| T2 → T4 | `payment_ref`, `payment_checkout_url` written by the student's token on a same-status update | Trigger's same-status branch permits both forward from null only. Agree. |
| T2 → T5 | money columns service-role only | Webhook uses the service-role client. Agree. |
| T2 → T10 | `amount_paid_paise`, `refund_ref` | History reads both. Agree. |
| **T3 → T8** | **stub `createCheckout` returns a `/dev/checkout` URL** | **CONFLICT — no task creates that page, so clicking Pay 404s and the stub cannot be walked end to end, defeating its purpose.** See Ruling R2. |
| T3 → T5 | `verifyWebhook(rawBody, signature)` | T5 reads `x-payment-signature`; stub's `signForTest` returns hex; T5 passes the raw body. Agree. |
| T4 → T8 | `{ checkoutUrl } | { error }` | T8 narrows with `"error" in result`. Agree. |
| T5 → T10 | webhook writes `amount_paid_paise` → history sums it | Agree. |
| T6 → T8 | `verifyPaymentNow(sessionId)` | Agree, but see Ruling R3 — T6 does not actually verify anything yet. |
| T7 → T9 | `acceptSession` stops redirecting | T9's handler clears `busy` and lets the UPDATE listener move the prompt. Agree. |
| T7 → T8 | waiting page `.select()` needs `hourly_rate` (T8) and `payment_deadline` (now T1) | Both additions land; no conflict, but T8 must not assume T1 added `hourly_rate` — it did not. Noted in T8 Step 1. Agree. |
| T2 → T11 | trigger → probes | T11 attacks exactly the columns and transitions T2 restricts. Agree. |
| T12 → T3, T6 | real adapter replaces stub; adds `fetchPayment` | Agree, once R3 is recorded. |

### Per-task self-consistency rows

| Task | Tests vs code it specifies | Finding |
|---|---|---|
| T1 | 5 describes vs 4 new exports + 2 modified | Arithmetic checked by hand: `paymentDeadlineFrom(12:00:00Z)` = 12:02:00Z at 120s ✓; `amountPaiseFor(500,20)` = round(166.67)=167 → 16700 ✓; `(500,30)` = 25000 ✓. Agree. |
| T2 | DDL vs verification SQL | The inline verify block is thin — one line is a no-op `where false`. See Ruling R4. |
| T3 | stub tests vs stub code | `signForTest` present, tamper and wrong-signature both covered, empty-secret refused. Agree, once R2 adds the page. |
| T4 | reuse path returns before computing the amount | Correct ordering; no wasted port call. Agree. |
| T5 | fields used vs fields selected | `hourly_rate`, `duration_minutes`, `payment_ref`, `status` all in the select. Agree. |
| T6 | claims a dual path, implements a log | See Ruling R3. |
| T7 | imports dropped vs added | Drops `createSessionRoom`; adds `paymentDeadlineFrom`, `expiredAcceptedIds`; guard becomes `canTransition("pending","accepted")`. Agree. |
| T8 | `Shell` extraction | Not yet written anywhere; the task specifies extracting it from existing markup. Adequate instruction. Agree. |
| T9 | prompt state machine | `accepted` keeps the prompt, `paid` keeps it, anything else clears it. Agree. |
| T10 | earnings filter | `refund_ref === null` AND `amount_paid_paise !== null` AND derives to `completed`. Matches invariant 4. Agree. |
| T11 | probes vs what T2 restricts | Four attacks map to the four service-role-only columns/transitions. Agree. |
| T12/T13 | blocked task + verification | Agree. |

## Rulings

Ruling R1: Task 1 now also adds `payment_deadline` to all 7 `effectiveStatus` call sites, so its commit type-checks. The plan originally deferred those edits to Task 7, leaving an intermediate commit that fails `tsc` — unbisectable, and it would have forced Task 1's reviewer to accept a red type-check as "expected". Task 7 keeps only the behavioural change (widening the student's status filter). Cost if wrong: Task 1 touches 5 more files, all one-line select-string additions, slightly widening its review surface.

Ruling R2: Task 3 also builds `src/app/dev/checkout` (page + server action). The stub's `createCheckout` returns a URL to that route and no task created it, so Pay would 404 and the flow would be unwalkable — removing the only reason the stub exists. Both the page and the action hard-refuse when `NODE_ENV === "production"`, and Task 13 gains a step asserting the route 404s in production. Cost if wrong: one throwaway dev-only route to delete at launch, already flagged in the task text.

Ruling R3: Task 6's `verifyPaymentNow` ships as the hook plus a log line, NOT as a real verification — with a stub adapter there is no provider to query. The spec's §3.6 dual path is therefore genuinely incomplete until Task 12 Step 3b adds `fetchPayment` to the port and makes this drive the same idempotent transition the webhook drives. Recorded in the plan at both ends so nobody marks §3.6 satisfied on the strength of a log line. Cost if wrong: between Task 6 and Task 12 there is one confirmation path, not two — acceptable because the stub cannot drop a webhook.

Ruling R4: Task 2's inline verification SQL stays thin and runtime proof stays in Task 11's probes, which test the trigger against real rows rather than hand-written throwaway fixtures. Applying the migration is user-owned and batched to one action, as M2's Ruling R5 established. Cost if wrong: a defect in the trigger surfaces at Task 11 rather than at Task 2 — one task later, still before any UI depends on it.

## Progress
Task 1: implemented (commit dfc72b8, 15 new tests, 72 total passing, tsc exit 0, eslint clean) — status DONE_WITH_CONCERNS. Review dispatched over ff3e45c..dfc72b8.
Task 1: implementer concern 1 — it changed the existing `canTransition("pending","active")` test to `("pending","accepted")`, which the new ALLOWED table required. Correct as far as it goes.
Task 1: CONTROLLER-CONFIRMED GAP (found before review returned, verified by grep): src/app/dashboard/actions.ts:40 still calls `canTransition("pending","active")`, which the new table makes FALSE — so acceptSession would return "Invalid transition." for every accept, at runtime, while tsc and all 72 tests stay green. The implementer fixed the test that encoded the old rule and left the production caller. This is the same failure class as the plan's own warning about opaque `.select()` strings: a change to a lookup table is invisible to the compiler. Task 7 rewrites this line anyway, but a commit that silently kills the accept path is not a commit to mark complete. Enters the fix loop with whatever the reviewer returns.
Task 1: implementer concern 3 — teachers/actions.ts still filters `.in("status", ["pending","active"])`. Correctly deferred: the plan assigns that widening to Task 7 Step 2. Not a defect.
Task 1: review returned — spec ✅ (every brief requirement verbatim; reviewer independently re-ran tsc/tests/eslint, hand-checked the money arithmetic, and grepped src/app to confirm the seven .select() sites are exhaustive), task quality Approved, zero Critical/Important in Task 1's own diff. Reviewer independently CONFIRMED the acceptSession break by executing canTransition("pending","active") against the new table, and confirmed it is the only other caller in the codebase.
Task 1: Ruling R5 — the reviewer attributed the broken accept path to intentional plan sequencing and recommended only a "do not merge mid-sequence" note. I am overruling that and fixing it in Task 1 instead. Reasoning: this is the identical failure class to Ruling R1 — a change invisible to the compiler (a lookup table, exactly like an opaque .select() string) that leaves a commit type-checking but functionally dead. R1 already established that Task 1 owns the call sites it breaks; a guard that kills the accept path is no different from a select that silently disables the read-time rule. The one-line fix restores acceptSession to its pre-branch behaviour, which the currently-applied 0003/0004 trigger still permits, and Task 7 rewrites the block regardless. Cost if wrong: one extra fix round on a line Task 7 replaces anyway, and Task 1's diff touches one more line of a file it already edits.
Task 1: fix round 1/5 dispatched to the original implementer — one-line guard change plus a new canTransition test asserting both removals and both new hops, so the regression is guarded rather than merely fixed. The old guard-test was rewritten during implementation, which is why nothing caught this.
Task 1: fix round 1/5 applied (commit b8cf5a9 — guard changed to canTransition("pending","accepted"), plus a new canTransition test asserting both removals and both new hops; 73 tests). Scoped re-review dispatched over dfc72b8..b8cf5a9.
Task 1: re-review clean — Finding 1 ADDRESSED (guard change verified as exactly one line, acceptSession's body byte-identical otherwise; the new test verified as four real assertions, not vacuous). No new breakage. Re-reviewer independently re-ran the three commands and matched 73/73, tsc 0, eslint clean.
Task 1: re-reviewer's coherence note, recorded as a KNOWN TRANSIENT: the code guard now says pending->accepted while the actual write still jumps pending->active, and the currently-applied 0004 trigger still permits that jump, so nothing rejects it. This is deliberately incoherent until Task 7 rewires acceptSession and Task 2's migration tightens the trigger. Flagged here so a later reader does not mistake it for a defect — and so that if the branch is ever paused, it is paused knowing the payment gate is not yet in the write path.
Task 1: complete (commits ff3e45c..b8cf5a9, 1 fix round, review clean)
Task 2: implemented (commit 365a46d, migration authored not applied per Ruling R4; 73 tests / tsc 0 / eslint clean, unchanged as expected for a SQL-only change). Implementer reports the Step 2 cross-check found exact agreement between the trigger's four transition clauses and ALLOWED across all 10 statuses, and that every guard from 0003/0004 was preserved — with one intentional narrowing, the teacher-answer rule no longer listing `active` as a directly settable target, since `active` is now reachable only via `paid`. Review dispatched over b8cf5a9..365a46d on the most capable model: this file is the milestone's security boundary, and the brief asked the reviewer to verify the preservation claim adversarially rather than accept it, to compare the transition table in BOTH directions (more-permissive-than-code is a hole, less-permissive is breakage), to walk seven concrete attacks, and to check whether paid_has_amount / refunded_has_ref can actually be satisfied in the order the webhook writes.
Task 2: review returned — spec ✅ on the transition table (verified exact in BOTH directions, target by target) and the preserved-clause audit came back with NOTHING dropped (the immutable-terms block is md5-identical to 0004's; the single narrowing of the teacher-answer rule is not merely intentional but REQUIRED, since leaving `active` in it would have blocked the service role). Reviewer also confirmed the payment_expired guard tests `old.payment_deadline` not `new.`, which is what stops a teacher dumping a student by rewriting the deadline in the same statement. Task quality: Changes requested. 3 Critical, 6 Important.
Task 2: C1 CONFIRMED EMPIRICALLY by the controller against the live DB before dispatching the fix — `add constraint paid_has_amount` validates against existing rows, and the live table holds 1 `completed` row from M2's verification run whose amount_paid_paise would be null. The migration WOULD have aborted on paste. This is precisely the "correct in principle, impossible at runtime" class the review brief asked about, and no test in this repo could have caught it.
Task 2: Ruling R6 — resolve C2, C3 and I1 with ONE change rather than three patches: every payment column (payment_ref, payment_provider, payment_checkout_url, amount_paid_paise, refund_ref) becomes service-role-only on BOTH the insert and update paths, in all branches. Reasoning: the reviewer found the insert path entirely unguarded (a student could POST a forged amount_paid_paise and poison the earnings figure §6 invariant 4 depends on), and found the write-once guard unreachable during the pending->accepted transition (a self-serve teacher could set payment_checkout_url in their accept PATCH and point the student at an off-platform payment page — a phishing vector, not a trusted-insider assumption). It also found payment_provider split across two authorities so that no caller could execute the statement at all. Making all six service-role-only closes all three at once. Consequence: checkout creation holds the service role for its one write, which amends design spec §3.5's "the webhook is the only thing holding the service role" to "the webhook and checkout creation". Task 4's plan text updated accordingly. Cost if wrong: one Server Action elevates for a single write on a row whose ownership it has already verified.
Task 2: Ruling R7 — I6 (application ordering) is mine, not the implementer's. 0005 deletes the pending->active edge that src/app/dashboard/actions.ts still writes, so applying it before Task 7 lands would break accept in production. The migration must NOT be handed to the user until Task 7 is complete; Task 11 is the natural point. Recorded so a compaction cannot lose it. Cost if wrong: production accept breaks between the paste and Task 7.
Task 2: fix round 1/5 dispatched — C1 (not valid + real pre-flight census), C2+C3+I1 (Ruling R6), I2 (unique index), I3 (transaction wrapper), I4 (bound the payment_deadline value), I5 (constraint-name pre-flight + if exists), plus four accepted Minors.
Task 2: minor (deferred): terminal rows are status-frozen but not column-frozen — payment_ref/payment_checkout_url remain settable on a completed or cancelled row where they are null. Low impact; noted rather than fixed.
Task 2: minor (deferred): the service role cannot set `completed` (uid is null is distinct from both participant ids). Pre-existing from 0003, not a regression, but a reconciliation or cleanup job will hit it — worth knowing before one is written.
Task 2: fix round 1/5 applied (commit af067ab). Controller spot-checks confirm the shape: begin/commit wrapper present (2 matches), paid_has_amount is NOT VALID at line 46, the payment_ref index is unique + if-not-exists at line 67, and the inert VERIFY block is replaced by a real PRE-FLIGHT at line 273. Implementer re-ran the Step 2 cross-check after restructuring the guards and reports it still matches ALLOWED exactly in both directions. Scoped re-review dispatched over 365a46d..af067ab on the most capable model, because the fix restructured the security guards rather than patching one line. The re-review brief's single most important instruction: walk every write the APPLICATION actually makes and flag any that the new rules block, because a migration that blocks a legitimate write is worse than the bug it closed — that is the failure mode this restructuring could plausibly have introduced.
Task 2: re-review verdict — ALL FINDINGS ADDRESSED. C1, C2+C3+I1, I2, I3, I4, I5 and the three Minors each closed by the mechanism the ruling asked for. All four attacks refused, each by a named clause. Critically: all NINE legitimate application writes verified still permitted (student insert, teacher accept with deadline, checkout creation under the service role, accepted->paid, paid->active, paid->refunded, the late-payment same-status write, active->completed, and the settle-on-read accepted->payment_expired) — the restructuring did not over-reach. Transition table still exact in both directions; immutable-terms block still byte-identical to 0004 by checksum.
Task 2: Ruling R8 — residual risk 2 is carried into Task 4's plan rather than left as a note. The new `paid_has_ref` constraint makes checkout creation's payment_ref write a HARD PRECONDITION for the webhook's accepted->paid transition: if that write silently lands on zero rows, the student pays and the webhook is then refused, so money is taken and no session is recorded. Task 4 now checks the update's row count and refuses to return the checkout URL if it did not stamp. This is a constraint the fix round created and that nothing downstream would otherwise have noticed. Cost if wrong: one extra round-trip check on the checkout path.
Task 2: residual risk 1 accepted for a fix round — `accepted_has_payment_deadline` was added validated while its twin got NOT VALID. Controller verified against the live DB that there are currently ZERO `accepted` rows (census: cancelled 1, completed 1, declined 1, timed_out 2), so it would not abort today. Fixing anyway because the file outlives this session: a future applier without that census in hand has a trap, and the pre-flight's annotation only tells them to look for paid/active/completed.
Task 2: residual risk 3 (deferred): `add column` and the `add constraint` statements lack if-not-exists, so a genuine partial prior run would abort a re-run. Largely moot now that the file is wrapped in a transaction — nothing it does can leave a partial state. Not fixed.
Task 2: minor (deferred): the PRE-FLIGHT block sits below `commit;`. It is commented out so a whole-file paste never runs it, but the ordering reads oddly.
Task 2: fix round 2/5 applied (commit 87ef03e — accepted_has_payment_deadline made NOT VALID, PRE-FLIGHT annotation widened to name `accepted`; 11 insertions, 3 deletions, SQL only). Scoped re-review dispatched over af067ab..87ef03e on a mid tier, sized to a two-edit diff. The re-review brief's emphasis is the scope check: round 1's restructuring was verified correct across four attacks and nine legitimate writes, so any drift into the trigger functions, the transition table, the column definitions or application code in this round is a NOT ADDRESSED, not a bonus.
Controller commit 52432e9: Task 4's plan text hardened per Ruling R8, committed separately so it stays outside the review range.
Task 2: re-review round 2 clean — Finding A ADDRESSED (not valid + real reasoning cross-referencing its twin), Finding B ADDRESSED (annotation names accepted and explains that pre-existing rows are expected). Scope check: nothing outside the two edits changed, confirmed by reading the full diff rather than the stat line. Re-reviewer also put on record what NOT VALID does and does not skip: it skips only the one-time retroactive scan; every future insert and update is still checked. That matters because "NOT VALID" reads like "unenforced" to anyone skimming, and this is the security migration.
Task 2: complete (commits b8cf5a9..87ef03e, 2 fix rounds, review clean)
Task 3: implemented (commit 331927c, 6 new tests, 79 total; tsc 0, eslint clean, build clean with /dev/checkout in the route table). Implementer confirmed all three production refusals throw under NODE_ENV=production via a throwaway vitest run, deleted before commit. Two non-blocking notes, both correct: next-env.d.ts left uncommitted (Next rewrites it after a build — the known flip-flop), and payNow's POST target /api/payments/webhook does not exist until Task 5, handled gracefully by design.
Task 3: review dispatched over 52432e9..331927c. The brief's emphasis: the stub holds the one capability that turns a session paid, so beyond confirming the three refusals exist, the reviewer is asked whether ANY path reaches signing or a paid session without passing one — specifically a production deploy with PAYMENT_PROVIDER simply unset, a direct import of stubPort bypassing getPaymentPort, and whether putting signForTest on the shared PaymentPort interface invites a real adapter to expose it later. Also asked to judge the stub's verifyWebhook as the reference implementation a real adapter will be written against, not merely as test scaffolding.
Task 3: review returned — spec ✅ (all five files byte-for-byte the brief's code; PaymentPort matches spec §3.4 and matches what Tasks 4 and 5 actually consume, checked against their briefs), zero Critical, 3 Important, 2 Minor. Task quality: Changes requested, narrowly.
Task 3: the reviewer verified something STRONGER than the implementer's own worry — /dev/checkout bakes to a static 404 artifact at build time, not merely a runtime check, because the guard fires before any dynamic API is touched and `next build` runs with NODE_ENV=production internally. Confirmed by inspecting .next/server/app/dev/checkout.meta (status 404, digest NEXT_HTTP_ERROR_FALLBACK;404). Recorded because it is a real strengthening of the security story and would otherwise be lost.
Task 3: Ruling R9 — the reviewer's central finding is accepted and is the reason this task does not pass yet. The three production refusals are all EXTERNAL to the capability they guard: stubPort() itself has no environment check, only `if (!secret) throw`. So the design is "three callers that each remember to check NODE_ENV", not "a capability that cannot be reached in production" — which is what the task's own comment claims. Any future direct import of stub.ts mints a valid signature with nothing firing. Fix round adds a self-check inside stubPort, making the fourth refusal the thing itself. Cost if wrong: two lines and one extra throw on a path that should never execute.
Task 3: Ruling R10 — signForTest comes off the shared PaymentPort interface. It is the contract Task 12's real adapter is written against, and a member only a stub should have does not belong on it: the comment saying "absent on real adapters" is documentation, not enforcement, and a future adapter author could implement it for local convenience and ship a live signing bypass. Replaced with a StubPaymentPort extension plus a separate getStubPort() that the dev checkout action calls. Cost if wrong: one extra exported type and one extra accessor.
Task 3: Ruling R11 — the reviewer correctly cited CLAUDE.md's requirement that a shortcut with a security cost in service is recorded in the spec's "Spike → production hardening" section, and the M3 spec has none. The stub plus /dev/checkout is exactly that class of shortcut. Adding §13 to the M3 spec in this fix round rather than deferring, because the whole point of that rule is that the debt is visible to someone reading the spec alone — which is how M2's §15 debt actually got closed. Cost if wrong: one spec section.
Task 3: fix round 1/5 dispatched — self-guard in stubPort, signForTest off the shared interface, spec §13, an empty-signature test, and permanent tests for the production refusals (the implementer had verified them with a throwaway file that was deleted, so the milestone's central security property was a memory rather than a test).
Task 3: re-review round 1 clean — F1-F5 all ADDRESSED at cited lines. The re-reviewer verified adversarially rather than by inspection: it DELETED the stubPort guard, confirmed the new test then failed with a real assertion error (not an uncaught throw), and restored the file. That is the check that distinguishes a load-bearing test from a decorative one.
Task 3: the NODE_ENV manipulation in the new tests was specifically scrutinised and judged sound — originals captured at describe-body evaluation before any mutation, Object.defineProperty with all three flags (required because Next types ProcessEnv.NODE_ENV readonly), afterEach restoring unconditionally including `delete` when the var was originally unset, and verified empirically to restore even when an assertion fails. No cross-file leakage. This mattered: a test that leaves NODE_ENV=production set would silently poison every later test file, which is worse than the bug it was added to catch.
Task 3: CORRECTION TO THE RECORD — the re-reviewer disputed the earlier claim that /dev/checkout bakes to a static 404, saying .next/server/app/dev/checkout.meta does not exist. The controller checked directly: the file DOES exist and reads "status": 404, and the baked checkout.html is a __next_error__ document. The original reviewer was right; the re-reviewer was looking at a stale or pre-build .next. The security story stands, now confirmed three independent ways (runtime guard, permanent test, build artifact). Recorded because a wrong correction left in a ledger is worse than the original error.
Task 3: complete (commits 52432e9..b2e7f30, 1 fix round, review clean, 83 tests)
Task 4: FIRST DISPATCH DIED — not a code failure. The agent hit an account-level session limit ("resets 6:30pm Asia/Calcutta") after writing src/app/session/payment-actions.ts but before verifying, committing or reporting. Controller inspected the untracked file directly: it is complete and matches the plan's code verbatim, including both rulings (R6's service-role client and R8's row-count precondition check).
Task 4: Ruling R12 — rather than re-dispatch a full implementer to re-derive work that is already correctly on disk, dispatched a cheap finisher whose only job is verify + commit + report, explicitly told not to rewrite the file and to change only what a failing command names. Reasoning: the expensive part of a dispatch is a fresh agent rebuilding context, and there is nothing left to decide here — the judgement calls were made and their output is sitting in the working tree, already read and confirmed by the controller. Cost if wrong: the finisher touches a file it did not author, so its report separates "verified" from "authored" and the task review still sees the whole diff on its own merits.
Task 4: NOTE FOR THE SESSION — subagent dispatch may be rate-limited until 6:30pm Asia/Calcutta. If the finisher also dies on quota, the remaining options are to pause execution until reset, or to continue with the controller implementing directly (which skips the review gate and should be a last resort, taken only with the user's agreement).
Task 4: quota resolved — user confirms the session limit reset at 6:30pm Asia/Calcutta, and the finisher dispatch went through cleanly, so full dispatch capacity is restored. The contingency noted above (pause, or controller implements without a review gate) is moot and was never exercised. Every task on this branch keeps its review.
Task 4: implemented (commit 210d7d9; 83 tests, tsc 0, eslint clean, build clean). Authored by the cut-off run, verified and committed by the finisher — so this diff has had LESS implementer scrutiny than the others, and the review dispatch says so explicitly and tells the reviewer not to assume any prior pass caught anything.
Task 4: review dispatched over b2e7f30..210d7d9. Emphasis: verify the two odd-looking design choices are CORRECT rather than merely necessary (the reviewer is asked to read 0005's trigger itself and confirm the user-token write would actually be refused — if it would not, the service-role elevation is unjustified and that is a finding); the resume path, which returns an existing checkout URL early and must not hand back a URL for an expired or already-paid session; and whether two rapid Pay presses can open two charges.
Task 4: review returned — APPROVED, zero Critical, 1 Important, 2 Minor. Both "looks like over-engineering" choices verified CORRECT against the migration rather than merely plausible: the reviewer read enforce_session_update itself and confirmed a user-token write to the payment columns raises 'payment columns are set by the payment webhook only' (a caller's token always carries a non-null auth.uid(); the service-role key has no sub claim so uid is null and the block is skipped), and confirmed paid_has_ref exists as claimed. Guard order verified: auth -> ownership (user's client) -> effectiveStatus -> resume check -> amount -> stamp -> confirm -> return. The resume branch correctly sits AFTER the status check, so a resumed checkout on an expired or already-paid session returns the error rather than a stale URL.
Task 4: fix round 1/5 dispatched — the stamp UPDATE discards its `error`, collapsing "the guard blocked this write" and "the database failed" into one branch with one message that asserts the former. A misconfigured service-role key in production would fail every checkout with a line reading like ordinary contention. Same class as M2's swallowed Daily 400, which cost a whole test round. Plus a one-line comment marking the narrow accepted window between the effectiveStatus check and the stamp's raw-column guard.
Task 4: minor (deferred): no idempotency key reaches port.createCheckout, so two rapid Pay presses both call the provider before the DB race resolves. No double-charge risk — only the write-winner's URL is ever returned — but a real provider would accumulate an orphaned checkout object per losing call. Not fixable in this file: CheckoutRequest has no such field. Carry into Task 12 when a real adapter defines what idempotency looks like for that provider.
Task 4: Ruling R13 — the reviewer flagged that the "student pays, webhook cannot record it" story depends on how Task 5 locates the row, which it could not verify because Task 5 does not exist yet. Controller checked the plan: Task 5 locates the session by `.eq("id", event.sessionId)` ALONE and never cross-checks the payment_ref it was handed. That means the event payload decides which session gets credited. Patched Task 5's plan to compare the row's stored payment_ref against the event's and auto-refund on mismatch, before the amount check. Reasoning: payment_ref is the half WE stamped and it is unique-indexed, so it is the trustworthy identifier; a signed event naming session A while carrying session B's charge reference is a replay, a provider bug, or worse, and crediting it would pay for a session nobody bought. Cost if wrong: one extra comparison and one more refund path on the webhook.
Task 4: fix round 1/5 applied (commit 509a743 — stampError destructured and logged distinctly with an early return before the zero-row branch; narrow-window comment added to the .eq("status","accepted") guard). Scoped re-review dispatched over 210d7d9..509a743. Task 5 brief regenerated AFTER Ruling R13's plan patch, so it carries the payment_ref cross-check.
Task 4: re-review — F1 ADDRESSED (stampError destructured, logged with the error object, early return placed before the zero-row branch; the two log lines confirmed genuinely distinguishable, and the zero-row message now means only what it says). F2 NOT ADDRESSED: the added comment restates the code ("a session could expire; accepting this window") without saying why that is tolerable. Scope check clean — guard order byte-for-byte unchanged from what the original review verified.
Task 4: Ruling R14 — F2 is parked, not re-dispatched. It is a Minor, and the skill's loop is for Critical/Important; spending a fix round plus a third re-review on the wording of one comment is churn against a milestone with nine tasks left. The reviewer's own analysis captures what the comment should have said and is preserved here for the final whole-branch review to triage: the window is bounded by a single provider round-trip against a 120-second payment deadline, and it is self-healing because every later read re-derives the true status through effectiveStatus rather than trusting the stored column — so the worst case is one stamped row that the next read already treats as expired. Cost if wrong: a comment in the money path explains less than it should; the behaviour is correct and verified either way.
Task 4: complete (commits b2e7f30..509a743, 1 fix round, 1 parked)
Task 5: implemented (commit b506ca9; 83 tests, tsc 0, eslint clean, build clean with /api/payments/webhook as a dynamic route). Status DONE_WITH_CONCERNS.
Task 5: Ruling R15 — the implementer found a genuine defect in the PLAN'S OWN verbatim code and, correctly, preserved it and flagged it rather than silently patching (it had been told not to simplify or reorder any guard). The duplicate-delivery short-circuit lists only ["paid","active","completed"], so a redelivery arriving after either refund path has already run falls through and calls port.refund() a SECOND time against the same charge. Controller analysis found this is worse than the implementer reported: it affects BOTH refund paths, not just the mint-failure one. A mint failure moves the row to `refunded`; a late payment keeps `payment_expired` and only records refund_ref — so no status list can catch both. The fix keys on refund_ref, which is set by both paths: a row that already carries one has, by definition, already been given back. Plan patched (select gains refund_ref; short-circuit gains the check) and a fix round dispatched. Cost if wrong: one extra column in the webhook's select and one extra condition on a short-circuit that already existed.
Task 5: FULL REVIEW returned (opus) — 5 Critical, 4 Important, 5 Minor. Task quality: Changes requested. The reviewer states explicitly that almost every defect originates in the PLAN'S verbatim code, not the implementer's transcription. Spec-compliance table, 8-attack walkthrough and a complete invariant-3 audit all included. Attacks 1a, 1b, 2, 4, 5, 7a, 7b, 8 all refused; the CAS at the claim confirmed a genuine row-level compare-and-swap under READ COMMITTED, not a re-read.
Task 5: C1 (the alarming one) — refundAndRecord's write had NO guard on which row or what state. Two consequences. (a) The payment_ref mismatch check sat ABOVE the resolved short-circuit, so an event carrying a foreign reference for an already-active session refunded the stray charge and stamped refund_ref + a foreign amount_paid_paise onto the live row: a delivered, genuinely-paid lesson then reads as refunded and invariant 4's earnings sum is wrong. (b) On an `accepted` row the same write left status=accepted + refund_ref, so the student's REAL payment then passed every check and hit round 1's refund_ref short-circuit — money taken, session never activated, never refunded, nothing logged. Round 1's fix is what converted (b) from bookkeeping damage into silent theft. This is the strongest argument in the session for re-reviewing fixes rather than trusting them.
Task 5: C2 — both refund branches sat above the short-circuit, so a retried mismatch called refund() on every delivery and rewrote refund_ref each time (0005 write-once covers payment_ref and payment_checkout_url only). On a real provider a second refund of a fully-refunded charge errors, poisoning the REFUND FAILED alarm that §9 says a human must act on.
Task 5: C3 — listing `paid` in the redelivery short-circuit made every mid-flight crash permanent. A function killed between the claim and the activation sends no 200, so the provider WILL retry, and createSessionRoom is idempotent precisely so that retry heals it. The short-circuit refused the one guaranteed repair.
Task 5: C4 — not one of the file's four Supabase results had its error inspected. Confirmed against postgrest-js: without .throwOnError() both PostgREST errors and network failures resolve as {error}, so the try/catch gave no cover at all. Same defect Task 4 was pulled up for and fixed; the precedent was not carried across.
Task 5: C5 — `if (!claimed || claimed.length === 0) return ok()` conflated three outcomes. A claim that ERRORED returned 200, so the provider marks the event delivered and never retries, the row stays `accepted` and amount_paid_paise stays null — meaning nothing in the database records that money moved, and reconciliation keys on exactly that column. The reviewer called it the darkest hole in the file, and it is: invisible to every invariant we wrote to catch this class.
Task 5: Ruling R16 — rather than patch five findings onto a structure that produced them, the webhook was REWRITTEN in the plan and the brief regenerated. Order is now: identity (payment_ref) -> resolution -> amount -> window -> claim -> mint -> activate, with every database result's error handled and our own failures returning 503 so the provider retries instead of 200 telling it we are done. `paid` deliberately falls THROUGH to mint-and-activate as the repair path. Cost if wrong: a larger fix diff to review than five targeted patches, mitigated by the re-review being scoped to it.
Task 5: Ruling R17 — a crossed payment_ref now writes nothing and refunds nothing; it logs loudly and returns. Refunding a charge we cannot attribute is its own risk, and stamping its amount on the named row is what caused C1. Recorded in spec §9 as a known gap needing a human, since money then sits with the provider. Justified because payment_ref is unique-indexed and write-once, so a mismatch should be impossible — its occurrence is itself the signal. Cost if wrong: a real crossed-reference event holds a student's money until someone reads the log.
Task 5: fix round 2/5 dispatched against the rewritten brief (293 lines). Plan and spec committed as b3acd3e.
Task 5: minor (deferred): admin() duplicates payment-actions.ts verbatim — one serviceClient() helper would give the service-role construction a single place to audit.
Task 5: minor (deferred): DAILY_API_KEY missing turns every payment into an auto-refund. A misconfigured deploy should fail loudly and retryably rather than quietly refunding a day's revenue. The 503-on-our-own-failure change reduces but does not eliminate this.
Task 5: minor (deferred): the port's verifyWebhook casts JSON.parse output to WebhookEvent with no runtime shape check, so a provider adapter returning loose types (amountPaise as a string) would fail the strict !== and refund a correct payment. Belongs in Task 12 when a real adapter defines its own types.
Task 5: re-review of the rewrite — C1-C5, I2, I4 ALL ADDRESSED and the new order (identity -> resolution -> writes) confirmed correct. Reviewer independently verified the byte-for-byte claim by extracting the plan's fenced block and diffing it against the committed file (MATCH, 10845 bytes both sides). It also answered attack 2 provably: skipping the amount check on the repair path IS safe, because `paid` is written in exactly one place, guarded on `accepted`, reachable only past the amount check, and 0005 makes hourly_rate/duration_minutes immutable — so recomputing would yield the same number. And it confirmed autoRefreshToken:false is a real improvement, not documentation: the default starts a background timer that in a serverless invocation is a dangling handle able to fire after the response is sent.
Task 5: THE REWRITE INTRODUCED FOUR NEW DEFECTS, two High. This is the risk a restructure always carries and is exactly why the re-review existed. (1) HIGH — the zero-rows re-read discards its error, so a transient read failure makes `resolved` true and returns 200: charged, not refunded, not claimed, not one log line. C4's defect class re-introduced in the one branch whose job is deciding whether to give money back. (2) HIGH — when the new `.is("refund_ref", null)` guard blocks the write, PostgREST returns 204 with error null and there is no .select(), so a refund that HAS been issued is never persisted and never logged. The guard I added to fix C1 created a new way to lose a refund. (3) MEDIUM — a refund that succeeds while its write fails leaves the row paid/refund_ref-null, so a later redelivery activates a session already refunded; unreachable before, because `paid` used to short-circuit. (4) MEDIUM — the repair path re-anchors started_at to the retry clock while createSessionRoom's fallback returns the FIRST mint's room with the FIRST mint's exp, so a repair more than ~15 minutes late gives a room that dies mid-lesson, and `active` has no route to `refunded`.
Task 5: Ruling R18 — defects 1, 2, 5, 6, 7 are fixed outright. Defect 3 gets one retry of the refund_ref stamp plus the existing loud log, not a durable fix: eliminating it needs a transactional outbox, which M3 does not have and which is disproportionate to a window requiring refund() to succeed while its write fails. Defect 4 is bounded rather than solved — the repair path now refuses to activate past payment_deadline + (ROOM_GRACE_MINUTES - 5), because beyond that the first mint's room cannot cover a session anchored at the repair's clock. Cost if wrong: a late redelivery refunds instead of delivering a lesson that might still have worked.
Task 5: Ruling R19 — two things deliberately NOT changed, and the implementer told not to "improve" them. A mint failure whose refund also throws still returns 200 rather than requesting redelivery: a retry could deliver a lesson that was in fact refunded, and with money the conservative choice wins over the recoverable one. And the amount-mismatch refund still leaves the row `accepted`, because 0005's trigger refuses `cancelled` from the service role (uid null is distinct from student_id, so the guard raises) and permits `payment_expired` only after the deadline. Cost if wrong: one lesson not delivered on a transient double failure; one row holding its teacher until the payment deadline elapses.
Task 5: spec §9 now records all four strand residues explicitly, names the reconciliation script as the backstop for every one, and says plainly that a durable fix needs a transactional outbox M3 does not have. Recorded because the alternative is four silent gaps in the money path that only re-derive themselves at 3am.
Task 5: fix round 3/5 dispatched.
Task 5: re-review of round 3 — all seven findings plus the payment-actions line ADDRESSED at the root. FIRST ROUND ON THIS FILE THAT INTRODUCED NO CRITICAL AND NO HIGH. The reviewer independently verified finding 4's premise against daily.ts (createSessionRoom's name-clash fallback GETs the existing room and never rewrites exp, so a repair genuinely inherits the first mint's expiry), confirmed the repair bound fails CLOSED on a null deadline, confirmed it cannot fire on a fresh claim (conjoined with alreadyClaimed, read before any write this invocation), and computed the margin: T <= firstMint + 13min against a room living to firstMint + 15min. Also audited all 15 console.* calls and confirmed none prints a signature, secret, body or API key — rawBody and signature are never logged.
Task 5: invariant 3 strand count holds at four, and one narrows: a paid row failing its activate write repeatedly used to sit at paid forever once the provider's retry budget ran out; it is now force-refunded to a terminal state once the repair bound passes. The new branch introduces no fifth class.
Task 5: Ruling R20 — a fourth and FINAL round was dispatched for three LOW/INFO items, against the usual rule that Minors do not enter the loop. Justification for the exception is N1 specifically: the stamp retry can raise a false "REFUND ISSUED BUT NOT RECORDED" alarm about a refund that was in fact recorded, because a transport error after a successful commit is indistinguishable from one before it. No money is at risk. But spec §9 was just amended to name the reconciliation script as the backstop for ALL FOUR remaining strand paths, so an alarm that cries wolf degrades the exact mechanism we documented as the safety net. Fixing the alarm is cheap; leaving it would hollow out a guarantee made one commit earlier. Also folded in: deriving the repair slack from PAYMENT_WINDOW_SECONDS rather than a bare `- 5` (the spec calls that constant the one most likely to be tuned, and tuning it past five minutes would silently make the bound unsafe), and correcting the report's path 11, which the new bound changed and which the round-3 section explicitly certified as unchanged. Cost if wrong: one extra round on the milestone's hardest file, and a slightly tighter repair window (13 minutes rather than 10).
Task 5: Ruling R21 — two items deliberately left, with the implementer told not to touch them. The repair bound's `?? 0` handles null but an unparseable date would yield NaN and fail OPEN rather than closed; unreachable today because the column is timestamptz and PostgREST always emits ISO. And the activate write still lacks a .select(), so zero rows is indistinguishable from success there; it is round-2 code, low impact because only this route can move a row off `paid`, and opening new surface in a final round is how a fifth defect gets introduced. Both recorded for the whole-branch review. Cost if wrong: two known-and-written-down fragilities survive into M3.
Task 5: fix round 4/5 dispatched — final round on this file regardless of outcome.
Task 5: round 4 re-review — F1 and F2 ADDRESSED, code sound, no functional regression. The reviewer verified the thing I was most worried about: F1's new database read does NOT destructure its error, but it fails SAFE by the library's own type contract (PostgrestResponseFailure guarantees data:null when error is set, so check?.refund_ref is undefined, the strict equality fails, and the code falls through to the alarm). It therefore CANNOT suppress a genuine REFUND ISSUED BUT NOT RECORDED. The fix for a false alarm did not create a false silence, which was the specific failure I asked it to hunt. Also confirmed the derived slack computes 13 minutes with today's constants, that refund() is still called exactly once outside the retry, that the bound still fails closed on a null deadline and never fires on a fresh claim, and that scoping the new check to `retried` leaves attempt-1 zero-row matches alarming immediately with no extra read.
Task 5: PARKED for the whole-branch review (round 4 was final by prior ruling; these are report text and one logging nicety, none functional) —
  parked 1: task-5-report.md:812-815 still certifies "activate failure" among paths "unchanged by this round". A redirect note was added at path 11 itself, but the sentence that made the false claim was never edited. Ruling: real but confined to a report; the whole-branch reviewer reads the code, and the ledger now carries the correction.
  parked 2: task-5-report.md:897 says PAYMENT_WINDOW_SECONDS is "180s today" — it is 120s; 180s is the migration trigger's separate outer validation bound. The computation two lines later correctly uses 120s, so the code is unaffected.
  parked 3: task-5-report.md:907-908 says 13 minutes is "slightly tighter than round 3's flat 10-minute slack" — it is looser, not tighter.
  parked 4: the new verification read in refundAndRecord should destructure and log its error for operator visibility. Safe today by the type contract, but no log line distinguishes "genuinely not found" from "the verification read itself failed", so an operator seeing the alarm cannot tell whether the check even ran.
Task 5: complete (commits d387835..ae2435c, 4 fix rounds, 4 parked). Three reviews on the most capable model, two of which found Criticals the tests, types and build could not.
Task 5: the plan's Task 5 code block has been synced from the final implementation, so a future session re-running this plan produces the reviewed code rather than the original draft that contained five Criticals.
Task 6: implemented (commit bdf9cde; 83 tests, tsc 0, eslint clean, build clean). Pure addition — controller verified the diff is all + lines, so createCheckout is genuinely untouched rather than merely reported so. Review dispatched over e682bab..bdf9cde, told explicitly NOT to report the deliberate incompleteness as a defect but to judge whether the code makes that state honest, and asked to defend a severity on the undestructured `.single()` error given this function returns void and drives nothing — the same pattern that was Important in the webhook may be Minor here, and the reviewer should reason about the difference rather than pattern-match.
Task 7 brief staged. NOTE: Task 7 is the gate on Ruling R7 — once acceptSession stops writing pending->active, migration 0005 becomes safe to apply, and it goes to the user with the PRE-FLIGHT census.
Task 6: review APPROVED, zero Critical/Important, 2 Minor. The reviewer reasoned about severity rather than applying the file's precedent mechanically: the discarded `.single()` error is the same SHAPE as the webhook's and createCheckout's, but Minor here because this function returns void and drives no state — nothing is corrupted and no money moves either way. It correctly identified that the cost today is purely observability, and that the pattern becomes consequential the moment Task 12 gives the function teeth. It also verified access control properly: createClient() uses the anon key with the caller's cookie session, so RLS gates the row before any app check, and the explicit student_id comparison catches the teacher case RLS alone would allow.
Task 6: Ruling R22 — both Minors are carried into Task 12's plan step rather than reopening an approved task. Neither has a live consequence (the function drives nothing), both originate in the brief's mandated code rather than implementer deviation, and Task 12 rewrites this function wholesale, so fixing them there is where they actually stick. The second is the sharper of the two: the incompleteness caveat sits INSIDE the body past three early returns, while the LEADING comment describes the dual path as though it were live — so a caller who reads the name and the leading comment, which is precisely the reader most likely to be misled, would conclude the re-check already happens. That is the exact risk Ruling R3 was recorded to prevent, resurfacing one layer down. Cost if wrong: the caveat stays sub-optimally placed until Task 12.
Task 6: complete (commits ae2435c..bdf9cde, review clean, 2 carried to Task 12)
Task 7: implemented (commit 9917141; 83 tests, tsc 0, eslint clean, build clean). Controller independently verified the milestone's central security claim by grep BEFORE dispatching review: src/app/api/payments/webhook/route.ts:338 is the ONLY production writer of status "active" (all other hits are test fixtures in session.test.ts); createSessionRoom appears nowhere outside the webhook; the guard reads canTransition("pending","accepted"); and teachers/actions.ts now filters .in("status", ["pending","accepted","paid","active"]). The old flow is genuinely severed.
Task 7: FIRST REVIEW DISPATCH DIED on an account-level session limit ("resets 11:30pm Asia/Calcutta") — the second quota failure this session, after the 6:30pm one. No mutation: reviewers are read-only and this one died while still reading files. Re-dispatching.
Task 7: review re-dispatched after the quota failure; user confirms the limit has reset. Brief tightened on the retry (relative paths, an instruction to read the diff and the two changed files first and consult the rest only as needed) so the reviewer spends its budget on judgement rather than re-deriving the milestone.
Briefs 8 and 9 staged.
Task 7: review returned — the two scoped files are correct (brief matched essentially verbatim, transition guard and written status agree, both settle sweeps guarded, payment_deadline's 120s sits centered in the trigger's 60-180s window and a violation would surface LOUDLY through the existing error branch rather than silently). Reviewer independently re-ran the active-writer census and confirmed webhook route.ts:338 is the sole production writer; it went further than asked and grepped every occurrence of "active" to verify the rest are reads, filters or type unions. 2 Important, 2 Minor. Changes requested.
Task 7: THE FINDING NEITHER I NOR THE IMPLEMENTER SAW — src/app/dashboard/incoming-request.tsx:147-156. `accept()` sets busy=true and only clears it in the error branch. That was correct while success meant redirect() threw and unmounted the component. Task 7 removed the redirect, so the component stays mounted with busy stuck true, and neither realtime handler resets it. The card disappears (the UPDATE handler clears it) so it LOOKS fine — but the next incoming request that teacher receives in the same page session renders with both buttons permanently disabled, no error, recoverable only by a full reload. A dormant defect in a file this diff never touched, exposed by its contract change. Both the implementer's report and my own dispatch framed this area as "no waiting state yet, deferred to Task 9"; it is not a missing feature, it is a live regression.
Task 7: Ruling R23 — findings 1 and 2 (discarded errors on the settle write-backs and on the openRows read) are fixed in a Task 7 fix round, since both are in Task 7's own files. Findings 3 and 4 (the stale comment and the busy-flag strand) are carried into Task 9's plan as explicit requirements rather than fixed here: Task 9 rewrites incoming-request.tsx wholesale to add the waiting state, so fixing it here would mean touching the same file twice and having the second pass overwrite the first. The branch is not deployed, so the strand cannot reach a real teacher before Task 9 lands. Cost if wrong: the defect exists on the branch for one task's duration, now named in the plan, the ledger and Task 9's brief rather than carried silently — which is the part the reviewer objected to, correctly.
Task 7: fix round 1/5 dispatched for the two error-handling gaps.
Task 7: re-review APPROVED. F1 and F2 both ADDRESSED. The fail-closed check passed cleanly and explicitly: both new branches test the `error` field, never data emptiness, so a successful read returning zero rows — the overwhelmingly common case — still falls through to the accept. That was the failure I was most worried about, because it would have killed the happy path while all 83 tests stayed green, none of them exercising these functions. Settle-write failures log without returning, so a failing write-back cannot block an accept the read-time rule already permitted.
Task 7: complete (commits c571596..8688dec, 1 fix round, review clean)
RULING R7 IS NOW DISCHARGED — acceptSession no longer writes pending->active, so migration 0005 is safe to apply. Handing it to the user with the PRE-FLIGHT census.
MIGRATION 0005 APPLIED to the live project (2026-08-26, user-run) and VERIFIED. Controller ran the PRE-FLIGHT census first (cancelled 1, completed 1, declined 1, timed_out 2 — the single `completed` row being exactly why both new constraints are NOT VALID). All six new columns readable after apply.
SECURITY PROPERTY NOW TESTED, NOT MERELY READ. Ad-hoc M3 attack probe seeded a real session, had the TEACHER legitimately move it pending->accepted with a payment_deadline (which SUCCEEDED, proving the lockdown did not overshoot), then attacked it with that same teacher token straight through PostgREST. All seven refused, each by the intended clause: mark it paid -> "payment columns are set by the payment webhook only"; jump to active -> "illegal session transition accepted -> active"; write an amount never charged, forge a refund_ref, mark it refunded, and point the student at an off-platform checkout url -> all the payment-columns clause; expire the window early to dump the student -> "a payment can only expire after its deadline". Table returned to 5 rows.
Of those, the off-platform checkout url is the one that matters most historically: it was a live hole (Task 2 review, C3) where a self-serve teacher could hand their student an attacker-controlled payment page inside the accept PATCH. It is now refused by the database rather than by convention.
Task 11 still owns formalising these into committed probe files plus the full nine-write regression probe and the reconciliation script; this run was controller verification of a user-applied migration, in the same manner as 0003 and 0004.
Task 8: implemented (commit 7c0787c; 83 tests, tsc 0, eslint clean, build clean). Review APPROVED, zero Critical/Important, 2 Minor.
Task 8: the thing the review brief was most worried about did NOT happen, and the credit is the implementer's. Expiry now means two different things — while `pending` it is the teacher not answering (timed_out), while `accepted` it is the student not paying (payment_expired) — and ALLOWED permits only the first. The implementer recognised this unprompted, diverged from the brief's literal "record the outcome then ask the server" comment, and gated timeOutSession to status === "pending", falling back to a plain router.refresh() for the payment window. The reviewer traced the hypothetical and confirmed the divergence was right: against an `accepted` row the action's own .eq("status","pending") guard would have matched zero rows, so it would have been a SILENT no-op that never even reached the trigger. Refresh instead rides the settle-on-read rule, effectiveStatus derives payment_expired, and the page guard redirects. Verified nothing downstream needs the column physically written: isBusy derives at read time, acceptSession's expiredAcceptedIds sweep settles it later with a logged failure, and earnings only count `completed` rows with an amount.
Task 8: Ruling R24 — fixing one Minor rather than deferring it, and this is the LAST such exception on this branch. pay() has no try/catch, so a THROWN exception from createCheckout's unguarded auth/session read (as opposed to the {error} it returns for anticipated failures) rejects the promise. With no error.tsx anywhere in src/app, that leaves `paying` stuck true and the button reading "Opening…" forever, recoverable only by reload — on the one screen where the student has already been told their teacher is waiting. CLAUDE.md's bar is that a shortcut causing a functional problem in service is not left silent, and a dead payment button is exactly that. Cost if wrong: three lines and one round on a low-likelihood path.
Task 8: minor (deferred): `left`'s useState initializer runs only on mount, so across a pending -> accepted transition the spinner number is stale for up to one 250ms tick. Real, imperceptible, and not worth touching a countdown that is now correct.
Task 8: fix round 1/5 dispatched.
Task 8: re-review PASS. Finding ADDRESSED at waiting-client.tsx:182-200 — try/catch present, catch logs the real cause then sets a user-facing sentence and resets paying. The check that mattered: window.location.href still sits inside the try AFTER the narrowing's early return, so the success path is unconditionally reached. The diff moved it inward one level and nowhere else — a fix that had relocated it into a branch would have replaced a rare dead-end with a total one, every student stuck instead of the unlucky few. Scope clean: three-state rendering, Shell, countdown deps and the timeOutSession gating all untouched.
Task 8: complete (commits f23ae1b..7700ff2, 1 fix round, review clean)
Task 9: implemented (commit e2f6a74; 83 tests, tsc 0, eslint clean, build clean). Status DONE_WITH_CONCERNS.
Task 9: MY BRIEF WAS WRONG, AND SERIOUSLY SO. Context item 5 asserted "The realtime subscription already exists and works. It carries the teacher to /call/{id} when the row reaches active. Do not rebuild it; extend what is there." That was false. Controller verified with git show 8688dec:src/app/dashboard/incoming-request.tsx — no router, no useRouter, no /call reference anywhere in the file, ever. In M2 the teacher reached the call solely via redirect() inside acceptSession (git show f10e4cf:src/app/dashboard/actions.ts:65), and Task 7 removed that redirect without anything replacing it. Consequence had the implementer trusted me: a teacher whose student PAID would watch the card vanish with no route into the session they were just paid for — the money moves, the room exists, and the teacher cannot reach it. The implementer verified rather than trusted, found the gap, added the navigation mirroring the student's waiting screen, and flagged the divergence explicitly.
Task 9: this is the THIRD brief error of mine caught downstream (after Task 5's five-Critical webhook draft and Task 7's incorrect framing of the busy bug as a missing feature). The pattern is consistent: every one was an assertion about code I had not re-read at the time of writing. Recorded so the whole-branch review weighs my briefs' claims accordingly, and so a future session writing plans in this repo treats "already exists and works" as a claim requiring a grep, not a memory.
Task 9: review returned — the two named defects (busy stuck on the success path, card cleared on accepted) are genuinely closed, the money boundaries hold (nothing here writes `active` or calls Daily), and the un-briefed navigation the implementer added was assessed on its merits and found correct: right condition, right id via the functional-updater match so a different pending request cannot trigger it, guarded against firing after unmount, and safe because the webhook writes status/started_at/daily_room_url in ONE update so the room provably exists by the time this observes `active`. 2 Important, 2 Minor. Changes requested.
Task 9: IMPORTANT 1 — the sharpest finding of the task, and a gap this task CREATED. The catch-up query on mount still asks only for status = "pending". That was correct before, because `accepted` was instantaneous under the old redirect, so no reload could land inside it. Making `accepted`/`paid` a real state lasting up to 120s without widening the recovery query means a teacher who reloads mid-payment-window mounts with request = null, the UPDATE handler's `prev.id !== row.id` guard then never matches, router.push never fires, and the teacher sits on an empty dashboard while a paid session runs without them. session-history lists it but links nowhere. The student's side is immune by construction because it is a server-rendered route that re-derives on every load; the teacher's is a client component holding state, and that asymmetry is exactly where the bug lives.
Task 9: IMPORTANT 2 — a thrown (rejected) server action still leaves busy stuck, reproducing the original symptom via a different trigger. The sibling pay() in waiting-client.tsx already guards this and names the unhandled-rejection risk in a comment; the pattern was not carried across. Fix uses try/catch/finally so the reset cannot be missed.
Task 9: fix round 1/5 dispatched (both Importants plus the one-line navigation latch, bundled because it is free). Parked: `paid` renders the same "waiting to pay" copy — brief-conformant, disposable screen, reviewer agrees it is not a defect.
Task 9: re-review APPROVED. F1, F2, F3 all ADDRESSED, verified by reading the code rather than the stated intent. The widened query was examined per-status: a pending row behaves byte-identically to before, an accepted row recovered on reload counts down against payment_deadline not accept_deadline, and — the failure I named as the easy way to get this wrong — the expiry discard IS applied per-status, so an accepted row is never thrown away for having a necessarily-expired accept_deadline. navigatedRef confirmed unable to block a later different session, because pushing to /call unmounts the component and a fresh mount gets a fresh ref.
Task 9: complete (commits 7700ff2..b6e5438, 1 fix round, review clean)
Task 9: Ruling R25 — the re-reviewer's residual finding is REAL and is a spec regression, recorded in spec §9 rather than patched. The catch-up query orders by created_at desc limit 1, so a teacher with an older `accepted` row and a newer `pending` row from a second student recovers the PENDING one on reload and masks the accepted one — reproducing exactly the stranding F1 just fixed. The reviewer traced reachability properly: requestSession only checks the student's own open sessions, the availability toggle keeps presence tracked through the whole accepted/paid window, and the online list filters on presence alone. Controller verified the spec claim: parent spec §125 and M2 design §3.1 BOTH require busy teachers be hidden, not greyed, "so every visible card is genuinely startable". That untracking is tied to `active`; M3 inserted accepted/paid ahead of it. So this is an M3 regression against a documented rule, not merely a query quirk. The root fix is presence — untrack on `accepted`, not on `active` — and patching the ordering would treat the symptom, which CLAUDE.md's fix-quality rule forbids. Sized as its own change because it touches the availability toggle and the presence lifecycle rather than the payment path. Cost if wrong: for one payment window a busy teacher looks startable and a second student's request can only fail; the stranding half is bounded by the same window.
Task 10: implemented (commit ff9f798; 83 tests, tsc 0, eslint clean, build clean). Review APPROVED on the arithmetic, 1 Important, 2 Minor.
Task 10: the reviewer verified the money maths properly rather than eyeballing it. Fetch scope `.in("status", ["active","completed"])` proved exactly sufficient by tracing effectiveStatus — "completed" can only be derived from a stored `active` row past its hour or returned verbatim from a stored `completed` row, so nothing that could count is excluded and nothing wasteful is included. Rounding proved LOSSLESS rather than merely safe: every amount_paid_paise reaching this query was produced by amountPaiseFor, which rounds to whole rupees BEFORE multiplying by 100, and the webhook refuses any amount that does not exactly match it — so every summed value is a multiple of 100 paise and Math.round is a no-op. Queries confirmed genuinely independent, no shared limit.
Task 10: Ruling R26 — CORRECTION TO MY OWN BRIEF, and to the implementer's report which repeated it. I told the implementer that the webhook's "payment arrived while status was X" branch leaves the row's status intact. It does not; it explicitly writes status: actual. The branches that genuinely leave status untouched are amount-mismatch and lost-claim-race (nextStatus: null), and both only ever fire on `accepted` rows. So against the CURRENT webhook, no row can carry refund_ref while deriving to `completed`, and the refund_ref clause is a no-op relative to the status filter. It is still the right clause, for a better reason than either of us gave: migration 0005's trigger permits a SERVICE-ROLE write that leaves status unchanged at any status, which is exactly how a manual refund against an already-completed session would be recorded — the §9 no-admin-surface gap made concrete. The clause defends a path the schema deliberately allows. Cost if wrong: none to the code; the reasoning in the report and ledger is now accurate.
Task 10: fix round 1/5 dispatched — the earnings query discards its error, so a failed read renders ₹0 as fact on a payouts figure. Inherited verbatim from my brief. A wrong number that looks authoritative is harder to notice than a broken one, and it tells a teacher who has worked that they are owed nothing. Now logs and renders an em dash with a "couldn't load earnings" caption; a genuine zero still renders ₹0, because that is true.
Task 10: minor (deferred): the `amount_paid_paise !== null` filter clause does not change the sum — the reduce's `?? 0` already handles it. Harmless, documents intent, but the report overstated its function.
Task 10: re-review — fix ADDRESSED. Both ternaries key on billableError, never on the amount being zero, so a teacher with no completed sessions still correctly sees ₹0 while only a failed read shows the dash. Log format matches the codebase convention. Arithmetic and the rows-shown query byte-for-byte unchanged. The re-reviewer independently re-verified BOTH of my corrections against the actual code: the "payment arrived while status was X" branch does explicitly write status (so my original claim was wrong and the correction is right), and migration 0005's trigger genuinely permits a service-role same-status write of refund_ref at any status including completed (so the replacement justification is right).
Task 10: parked — the task-10 report is internally inconsistent: the original wrong explanation still stands at line 51 with the correction appended at line 96 rather than replacing it, so a reader going top-to-bottom meets a false claim stated as fact before reaching its retraction. Ruling: not worth a round. That report lives in the gitignored SDD workspace which is deleted when the branch finishes; this ledger carries the corrected reasoning and is what survives. Recorded so the discrepancy is not mistaken for a live disagreement.
Task 10: complete (commits 5ca1a16..6f945de, 1 fix round, review clean, 1 parked)

================================================================================
SESSION END — 2026-08-27. M3 at 11 of 13 tasks. Branch m3-payments, 35 commits
ahead of main, NOTHING PUSHED. Tree clean. 83 tests / tsc 0 / eslint clean /
build clean. Read project_state.md first; this file is the detailed audit trail.
================================================================================

STATE OF PLAY
  Tasks 1-10 complete and reviewed. Task 11 implemented and committed (490efbd);
  its task review was IN FLIGHT when the session ended — verdict unknown. A fresh
  session must check whether findings came back before treating Task 11 as closed.
  Task 12 (Razorpay adapter) is blocked on the user supplying test-mode keys.
  Task 13 needs the user's two-browser run.

WHAT WAS PROVEN, NOT JUST WRITTEN
  Controller ran probe-session-rls.mjs and reconcile-payments.mjs directly against
  the live database after migration 0005 was applied. All ten attacks refused —
  mark paid, jump to active, forge an amount, forge a refund_ref, mark refunded,
  point payment_checkout_url off-platform, expire the window early, and rewrite
  hourly_rate / subject / accept_deadline. Table returned to exactly its original
  5 rows. All four money invariants hold. Both exit 0.

THE PATTERN WORTH CARRYING FORWARD
  Four separate times, a claim of mine turned out to be wrong about code I had not
  re-read at the time of writing: Task 5's webhook draft carried five Criticals;
  Task 7's brief framed a live regression as a missing feature; Task 9's brief
  asserted a navigation path that had NEVER existed, and following it literally
  would have left a paid teacher with no route into their own session; Task 10's
  brief mis-attributed which webhook branch preserves status. Every one was caught
  downstream — three by reviewers, one by an implementer who verified instead of
  trusting. A future session writing plans in this repo should treat "already
  exists and works" as a claim requiring a grep, not a memory.

  The webhook alone took four fix rounds and three reviews on the most capable
  model, and nine Criticals were found across the milestone. None of them would
  have been caught by tests, types or the build — they were found by reading, and
  by attacking a real database.

SECURITY ACTION FOR THE USER
  Rotate tutor-check@smbtutorials.in's password. It was set during this work and
  passed through conversation transcripts. It is in NO committed file — the probes
  read it from PROBE_TEACHER_PASSWORD and fail loudly when unset, verified by grep
  — but it should not survive into anything beyond internal probing.

DECISIONS AWAITING THE USER
  1. Razorpay test-mode keys (blocks Task 12).
  2. The two-browser run (blocks Task 13).
  3. Whether to fix the busy-teacher regression inside M3 or let it ride into the
     redesign. It is a documented spec violation, not a defect of taste: two specs
     require busy teachers be hidden, and a teacher in the 120s payment window is
     not. Root fix is presence — untrack on `accepted`, not `active`.
  4. Whether to push branch m3-payments. Nothing has been pushed; the work exists
     only on this machine and in this ledger.

--- ADDENDUM, after the session-end block above ---

Task 11: review RETURNED AFTER the handoff was written. Verdict: CHANGES REQUESTED.
2 Important, 2 Minor. The security half was confirmed solid — all ten attacks are
genuine refusals verified live, each checked two ways (non-2xx AND a service-role
re-read confirming no attacked column moved, which closes the "trigger errors but
partially commits" gap). The reviewer reasoned the acid test properly against
0003: `accepted -> active` really WAS a legal teacher-permitted transition before
M3, so a reverted 0005 would flip that attack to PERMITTED and the script would
correctly exit non-zero. It re-ran all three scripts itself and matched the
output byte-for-byte, and confirmed the table at 5 rows before and after.

FIX ROUND 1 WAS DISPATCHED AND DIED ON AN ACCOUNT SESSION LIMIT (resets 4:30am
Asia/Calcutta). It changed nothing — verified: tree clean at 18e3582. The three
findings below are UNADDRESSED and are the first work a fresh session should do.

  FINDING 1 (Important) — `accepted -> cancelled` by the student is required by
  the brief and is tested nowhere. It is the ONE legitimate write in the whole M3
  surface this suite cannot prove: the trigger gates it on
  `uid is distinct from old.student_id` with NO service-role escape, unlike the
  payment-column gate, so the service role genuinely cannot stand in. It needs a
  real student JWT. FIX WITHOUT ASKING THE USER FOR A PASSWORD: create a
  throwaway student via POST /auth/v1/admin/users (email_confirm true, role in
  user_metadata), sign in via /auth/v1/token?grant_type=password, delete it in a
  finally — the pattern M2's probe-third-party.mjs used successfully. Generate
  the password at runtime; never write one down.

  FINDING 2 (Important) — the three malformed-insert probes never capture the
  created row's id, because they assume the insert fails. That section exists to
  catch a regression where one STOPS failing, and in that case the leaked row
  sits in the live table permanently carrying forged or inflated data, poisoning
  the row-count baseline for every later probe run and for reconcile-payments.
  The failure mode is worst exactly when the probe is doing its job. Fix: on
  res.ok, push the returned id into the cleanup list before logging.

  FINDING 3 (Minor, free once finding 1's throwaway student exists) — the attack
  probe runs only as the teacher. The brief and spec §8 both say "as the teacher
  and again as the student", and §8 calls it "the single most important test in
  the milestone". The reviewer verified the relevant trigger gates key on
  `uid is not null` with no role branching and the RLS UPDATE policy is
  symmetric, so this very likely proves the same path twice — but a future change
  that DOES branch on role would otherwise go unprobed.

  Also correct in the task-11 report: it justified skipping the student run by
  calling task-11-brief.md a stale M2 artifact. It is not — the reviewer read it
  and it is the real M3 brief containing that line.

  NOT a defect, noted for precision: two of the ten attacks (jump to ACTIVE, mark
  REFUNDED outright) are refused by the general transition-table gate rather than
  the payment-specific clause, because the row never reaches `paid` first. The
  refusals are genuine and the acid test still holds.

Task 11: NOT complete. Implemented and committed (490efbd), reviewed, fix round
owed. Everything else in M3 stands as recorded above.

--- FIX ROUND, 2026-08-27 (fresh session, after the quota-limited round changed nothing) ---

Task 11: FIX ROUND COMPLETE. All three review findings closed. Verified by
running every script against the live database, not by inspection.

  Process deviation, stated up front: project_state.md says to resume with
  superpowers:subagent-driven-development, but this session was configured
  not to dispatch subagents. The round was executed inline by the controller
  with the same discipline — implement, verify live, record here. No task
  review was dispatched for the fix round itself; the evidence is the probe
  output below, and Task 13's manual gate still sits downstream.

  FINDING 1 CLOSED — `accepted -> cancelled` by the student.
  probe-happy-path.mjs gains Row 4: the student inserts with their OWN token,
  the teacher accepts, checkout is stamped by the service role, and the
  STUDENT cancels with their own token. PERMITTED, live. This was the one
  legitimate M3 write the suite could not make, because the trigger gates it
  on `uid is distinct from old.student_id` with no service-role escape.

  FINDING 2 CLOSED — the malformed-insert leak. The three probes (now four)
  capture the returned id into the cleanup list before printing the verdict,
  so a row created by an insert that STOPS being refused is deleted rather
  than left in the live table poisoning every later row-count baseline.
  NOT just written — PROVED: a throwaway copy of the script made one of the
  four inserts deliberately legal. Result: the run reported 1 CHECK FAILED
  (so the regression is still loud) AND `sessions returned to its original 5
  row(s)` (so the leak is gone). Temp copy deleted.

  FINDING 3 CLOSED — the attack battery now runs as the student as well as
  the teacher, on two independently seeded rows so a permitted attack in the
  first battery cannot silently change what the second is testing. 20
  attacks, all refused, each still checked two ways (non-2xx AND a
  service-role re-read confirming no attacked column moved).

  BEYOND THE THREE FINDINGS — deliberate, recorded rather than silent:
  new scripts/probe-accounts.mjs mints a throwaway teacher AND student per
  run via POST /auth/v1/admin/users (email_confirm true, so no mail is sent;
  password from crypto.randomBytes, never logged, never leaves the process),
  sets the teacher's hourly_rate with the service role because
  handle_new_user does not, signs both in, and deletes both in a `finally`
  — a failed deletion increments the failure count and fails the run. Both
  probes now assert `profiles` returned to its baseline count as well as
  `sessions`.
    Why this went past the findings: the reviewer's own fix for Finding 1
    prescribed a throwaway STUDENT. Extending the same pattern to the
    teacher removed PROBE_TEACHER_PASSWORD entirely — which is what made the
    round verifiable at all, since this session had no teacher password and
    the alternative was to block on the user for a credential the ledger had
    already flagged for rotation. It also means rows 1-3's student insert now
    runs under a real student JWT instead of the service role, which is
    strictly stronger evidence and retires the stale header comment claiming
    "there is no student password in this environment".
  Also added: a fourth malformed insert — a request arriving with payment
  data already on it — covering 0005's new insert-trigger ban, which had no
  probe. Refused, live.
  Also fixed: the cleanup assertions printed through the attack verdict, so
  they rendered as "REFUSED  sessions returned to its original 5 rows",
  which reads as the opposite of what happened. They now print CLEAN /
  LEFTOVER.

  NEW FINDING, found while proving Finding 1 — RECORDED, NOT FIXED.
  `accepted -> cancelled` is in spec §3.1's table, in ALLOWED, and in the
  trigger — but NO APP CODE CAN MAKE IT. cancelSession
  (src/app/session/actions.ts) filters `.eq("status", "pending")`, and
  waiting-client.tsx renders Cancel only in the pre-accept branch, replacing
  it with Pay once accepted. A student who changes their mind inside the
  120s payment window has no exit but to let the window run down, holding
  the teacher for the full duration — which compounds the busy-teacher
  regression already in §9.
    Verified safe either way before deciding not to fix: the webhook's
    `payment arrived while status was ${actual}` branch refunds a payment
    landing on a row that is no longer `accepted`, so cancel-in-one-tab /
    pay-in-another returns the money rather than losing it.
    NOT fixed inside this round because it is a UI change on a UI the user
    has already decided is temporary, and because "widen the action" vs
    "narrow the spec" is a product decision, not a defect fix. Recorded in
    M3 spec §9 and in project_state.md's known gaps. DECISION OWED BY THE USER.

  VERIFICATION, this session, in this order:
    node scripts/probe-session-rls.mjs   -> exit 0, 20/20 refused, sessions
                                            5->5, profiles 2->2
    node scripts/probe-happy-path.mjs    -> exit 0, 16 legitimate writes
                                            permitted incl. the student
                                            cancel, 4 malformed refused,
                                            sessions 5->5, profiles 2->2
    node scripts/reconcile-payments.mjs  -> exit 0, all invariants hold
    npx tsc --noEmit                     -> exit 0
    npm run test                         -> 83 passed (6 files)
    npx eslint .                         -> clean (one unused-var warning
                                            introduced and fixed)
    npm run build                        -> clean
  probe-session-rls.mjs was re-run after its final lint edit, so the recorded
  result matches the committed file.

Task 11: complete.

--- TWO GAPS CLOSED, 2026-08-27 (user answered "fix both") ---

Not plan tasks. Both were recorded §9 gaps; the user was asked which to take
on while Tasks 12/13 stay blocked on them, and chose both. App layer only —
no migration, no DB change — so the Task 11 probes are unaffected and were
deliberately NOT re-run.

GAP A: a teacher in the payment window was still visible as available.
  Fixed at the root, as the spec insisted: presence follows the COMMITMENT,
  not the navigation. Until M3 the hiding was a side effect of accepting
  navigating into the call; M3 put 120 seconds in front of that, and the
  dashboard stays mounted throughout.
    - IncomingRequest gains onLiveSessionChange, derived from its own card
      state (`accepted` or `paid`) rather than signalled at each call site,
      so no path — realtime, catch-up query, or the countdown hitting zero —
      can forget to report it.
    - New DashboardLive holds that one fact for two siblings. A fragment, not
      a wrapper div: the dashboard's space-y-8 applies to direct children.
    - AvailabilityToggle untracks but does NOT unsubscribe, so the channel and
      the localStorage intent both survive and the teacher reappears on their
      own. A visibleRef records what was last applied so the effect cannot
      churn presence on every render. inSessionRef is synced inside the effect
      rather than during render, and the effect corrects the subscribe() ack
      either way if a session starts mid-handshake.
    - Three UI states, not two. "In a session" is not "Offline" — calling it
      Offline invites the teacher to toggle back on mid-window and undo it.

  TWO MORE DEFECTS IN THE SAME CODE, found while fixing it and fixed here:
    (a) The catch-up query took limit(1) on created_at desc, so a second
        student's newer pending row masked the teacher's own in-flight
        session, and the eventual `active` update then had no card to match —
        the teacher never reached their own call. Now pickOpenRequest
        (src/lib/session.ts, 10 unit tests, written before the implementation
        and failing for the right reason first) ranks commitment above
        recency and drops rows whose clock has run out. Recorded as defence in
        depth, NOT as the fix: with presence corrected the second request
        should not be sendable at all.
    (b) The card collapsed `paid` into `accepted`, so the payment-window
        countdown could clear a card whose student HAD already paid, stranding
        the teacher out of a session they had been paid for. `paid` is now its
        own card state with no countdown — the trigger only permits
        payment_expired out of `accepted`, so the deadline stops governing the
        moment money is in. Found because pickOpenRequest deliberately keeps a
        paid row past its deadline, which the countdown would have contradicted
        one render later; leaving that contradiction in would have been exactly
        the makeshift fix CLAUDE.md forbids.

GAP B: `accepted -> cancelled` was in the spec, in ALLOWED and in the trigger,
  but no app code could make it. The user chose to widen the product rather
  than narrow the spec.
    - cancelSession now filters .in("status", ["pending","accepted"]) and
      returns { cancelled: boolean } instead of void.
    - THE RETURN VALUE IS THE LOAD-BEARING PART, not the widened filter. The
      old caller navigated unconditionally. With Cancel present during the
      payment window, a student whose payment cleared in the same instant
      would have been walked off the one screen their room was about to open
      on — a paid session they never join. The screen now stays put on a
      false and lets the realtime update move it, and leavingRef is no longer
      latched before the answer is known (latching it early would have
      disabled that very handler).
    - Verified BEFORE making the change, not assumed: the webhook's
      `payment arrived while status was ${actual}` branch refunds a payment
      landing on a row that is no longer `accepted`, so cancel-in-one-tab /
      pay-in-another returns the money. Without that branch this change would
      have created a money-loss path.

  VERIFICATION: 93 tests (10 new) / tsc 0 / eslint clean / build clean.
  NOT PROVEN IN A BROWSER, stated plainly rather than implied: presence and
  the Cancel wiring are client-side realtime and nothing automated touches
  them. Task 13 gained Step 3b, which spells out the clicks — including
  deliberately racing pay against cancel and checking the provider dashboard
  for the refund. Do not treat either gap as verified until that runs.

--- 2026-08-27, user decision on the test teacher's credential ---

The SECURITY ACTION FOR THE USER above (rotate tutor-check@smbtutorials.in's
password) is CLOSED as "will not do", by the user's own call: it is a pseudo
account scheduled for deletion before launch, so rotating it buys nothing that
deleting it does not. Do not re-raise it.

Two preconditions that make this fine, both already true: nothing reads the
credential any more (PROBE_TEACHER_PASSWORD was removed in the Task 11 fix
round, so no probe breaks when the account is deleted), and the password is in
no committed file. Deleting the account remains on project_state.md's "Open
before real launch" list, which is the real action.

--- 2026-08-27, branch pushed ---

git push origin m3-payments -> 98b36ab..5d1348e, a fast-forward. Note what that
proves: the remote ALREADY held this branch at 98b36ab, so the handoff block's
"NOTHING PUSHED" and decision 4 ("Whether to push branch m3-payments") were
both stale — the work was not machine-local as recorded. No upstream was
configured locally, which is how the discrepancy stayed invisible. Upstream is
now set.

main is untouched at 5f1cc24, so this produces a Vercel PREVIEW deployment
only; production is unaffected. Pre-push checks: base confirmed current
(origin/main == merge-base, no rebase needed), and the full branch diff was
scanned for JWTs, rzp_/sk_ keys and inline passwords — clean. .env.local is
untracked.

--- TASK 12, 2026-08-27 ---

Task 12: implemented and committed (4facdf3). 113 tests (20 new), tsc 0,
eslint clean, build clean, LIVE test-mode probe green against the real API.
User supplied test-mode keys earlier the same day, unblocking it.

Executed inline by the controller, not dispatched — this session cannot spawn
subagents. No task review dispatched; the evidence is the live probe plus
Task 13's manual gate downstream.

WHAT THE LIVE CALL CAUGHT THAT THE MOCKS DID NOT
  `reference_id` is capped at 40 characters. The probe used `probe-<uuid>`
  (42) and Razorpay answered `400 reference_id: the length must be no more
  than 40`. A session id is a 36-char UUID, so the APP is fine — with four
  characters of headroom and only while nothing prefixes it. Every unit test
  in razorpay.test.ts had agreed with the over-long shape without complaint.
  This is precisely the failure class spec §8 exists for, and the second time
  in this project a live call has caught what a mock endorsed (M2's Daily
  payload was the first). Constraint now commented at the call site.

  Note also what the failure PROVED: a 400 with a field-validation message
  means Basic auth was accepted and the body parsed. Credentials work.

THE IDENTIFIER TRAP — RESOLVED WITHOUT A MIGRATION
  The plan called this "the one most likely to bite" and told the implementer
  to raise it rather than work around it. It resolves cleanly:
  createCheckout stamps the payment LINK id (plink_) as payment_ref, which is
  what the webhook's identity check matches on; refunds need the PAYMENT id
  (pay_), which does not exist until someone pays. refund() resolves one from
  the other via GET /v1/payment_links/{id}. One extra request on the rare
  path, payment_ref keeps its write-once guarantee, no 0006 migration.
  Verified live that an unpaid link already carries the `payments` field that
  resolution reads — its absence would have broken refunds only AFTER money
  had moved, which is the worst possible time to discover it.

PORT CHANGE: WebhookEvent.kind gained "ignored"
  Razorpay delivers payment.captured alongside payment_link.paid, plus
  refund.processed and disputes. verifyWebhook must not throw for those: the
  route answers 400 on a throw, Razorpay reads 400 as failure, and redelivers
  an event we were never going to act on, indefinitely. The stub never needed
  this because it only ever returned what we handed it — a good illustration
  that a stub cannot teach you a provider's contract.

STEP 3b — §3.6's DUAL PATH IS NOW REAL, AND IS ONE IMPLEMENTATION
  The webhook route's entire body after the signature check moved into
  src/lib/payments/settle.ts as settleVerifiedEvent(). PROVEN byte-identical,
  not eyeballed: extracted the pre-change body from git and diffed it against
  the new file — "IDENTICAL". route.ts is now signature-verification only.
  verifyPaymentNow asks the provider through the port's new fetchPayment and
  settles through that same function.
    Why not a second implementation: that logic took four fix rounds and nine
    Criticals. Two confirmation paths must not mean two copies of it.
  Task 6's two parked review findings landed here as promised: the session
  read now distinguishes a failed query from an absent row, and the leading
  comment describes what the function actually does rather than what it was
  going to do.

ALSO FIXED, not in the plan's file list: route.ts read only
x-payment-signature / stripe-signature. It now reads x-razorpay-signature
first. Without this every real webhook would have failed verification —
checkout working, student paying, nothing confirming.

NOT PROVEN, STATED PLAINLY
  A webhook signed by RAZORPAY (as opposed to our own HMAC round-trip), and a
  refund against a genuinely captured payment. Both need a real test payment
  in a browser, which is Task 13. The live probe file says so in its own
  header so a green run is not misread.

OUTSTANDING FOR THE USER
  Vercel env vars (PAYMENT_PROVIDER, RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET,
  PAYMENT_WEBHOOK_SECRET) for Production, Preview and Development — Task 12
  Step 2's second half. Local .env.local is done.

Task 12: complete.

--- VERCEL ENV + DEPLOYED WEBHOOK VERIFIED, 2026-08-27 ---

User added PAYMENT_PROVIDER, RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET and
PAYMENT_WEBHOOK_SECRET (All Environments), then NEXT_PUBLIC_SITE_URL
per-environment. Names cross-checked against `process.env.*` in src/ — all
match, no typos.

A FIFTH VARIABLE WAS MISSING AND WOULD HAVE COST THE TASK 13 RUN.
  NEXT_PUBLIC_SITE_URL was unset everywhere, so createCheckout fell back to
  "http://localhost:3000" and handed THAT to Razorpay as callback_url. A
  deployed payment would have succeeded and redirected the student to
  localhost on their own machine. Found by diffing the env names Vercel holds
  against the ones the code reads, not by reading the plan — the plan's Task
  12 Step 2 lists only the four payment vars.
  It is the only one that differs per environment (production URL / branch
  alias / localhost), so it must NOT be added as "All Environments".

A CORRECTION I HAD TO MAKE, worth carrying: I first told the user that curling
the deployed webhook unsigned would return 400 if configured and 500 if not.
That is WRONG and would have certified a broken deployment as working.
getPaymentPort() throws at construction when a credential is missing, and that
throw is caught by the SAME try/catch that rejects a bad signature — so a
completely unconfigured deployment also answers `400 bad signature`. Only a
correctly SIGNED request separates the two.
  scripts/probe-deployed-webhook.mjs now does it properly: signs a
  payment_link.paid payload for a random (nonexistent) session with the local
  PAYMENT_WEBHOOK_SECRET and asserts signed -> 200, tampered -> 400. Nothing
  is written; the session lookup finds nothing and the route returns ok().
  Plan gained Task 13 Step 8b for it.

RESULT, run against the preview deployment after a redeploy:
  correctly signed : 200 ok
  wrong signature  : 400 bad signature
  => the deployed adapter is Razorpay, and Vercel's PAYMENT_WEBHOOK_SECRET
     matches .env.local.

Also confirmed: env changes do not reach existing deployments, and
NEXT_PUBLIC_ variables are inlined at BUILD time — the first probe run failed
purely because the build predated the variables. An empty commit (bdaa702)
forced a fresh preview build; the probe went green 30s later.

--- TASK 13, AUTOMATED HALF, 2026-08-27 ---

Everything in Task 13 that does not need a browser is done and green. Only
the manual gate remains, and it is the user's.

  Step 1 (automated gate)      113 tests / 3 skipped (the live Razorpay probe,
                               correctly gated), tsc exit 0, eslint src/ clean,
                               build clean. tsconfig.tsbuildinfo removed.
  Step 2 (effectiveStatus)     RUN FOR THE FIRST TIME, and it passes. Every
                               real call site's query selects payment_deadline:
                               dashboard/actions.ts, dashboard/session-history.tsx,
                               call/[sessionId]/page.tsx, waiting/[sessionId]/page.tsx,
                               session/payment-actions.ts, lib/payments/settle.ts.
                               Two files matched the grep on COMMENTS only and
                               have no call — session/actions.ts:43 and
                               call/[sessionId]/call-frame.tsx:83. The call
                               page's other two .select() calls are on
                               `profiles` for full_name and are irrelevant to
                               the rule. No missed call site.
  Step 4 (reconciliation)      exit 0, all invariants hold, 5 rows, no
                               payment_ref anywhere yet (nothing has been paid).
  Live probes                  probe-session-rls exit 0 (20/20 refused, both
                               participants); probe-happy-path exit 0 (all
                               legitimate writes permitted incl. the student's
                               accepted -> cancelled, 4 malformed inserts
                               refused); both cleaned up, sessions 5->5,
                               profiles 2->2.
  Step 8b (deployed webhook)   signed 200 / tampered 400 against the preview.

WHAT IS LEFT IS EXACTLY THE BROWSER WORK, and nothing automated can substitute
for any of it:
  Step 3   the payment paths with Razorpay test instruments — and, critically,
           the first webhook RAZORPAY itself signs and the first refund against
           a genuinely captured payment. Neither has ever run.
  Step 3b  the presence fix and the payment-window Cancel added this morning.
  Steps 5-9 follow the run: confirm the data, update project_state, merge and
           push, verify production, re-run probe-deployed-webhook.mjs against
           PRODUCTION (nothing that passed on preview says anything about it),
           and confirm /dev/checkout 404s there.

REMINDER FOR WHOEVER RUNS IT: Razorpay's webhook currently points at the
PREVIEW url. That is correct for testing now and wrong the moment M3 merges —
it must move to https://smb-tutorials.vercel.app/api/payments/webhook.

--- SESSION END, 2026-08-27 (context exhausted) ---

M3 IS 12 OF 13. Branch m3-payments, pushed, tree clean. 113 tests / 3 skipped
(the live Razorpay probe, correctly gated) / tsc 0 / eslint clean / build clean
/ three live DB probes exit 0 / deployed webhook signed 200 tampered 400.

WHAT THIS SESSION DID, in order:
  1. Closed Task 11's owed fix round (3 review findings) and proved each live.
  2. Found and recorded a 4th finding: accepted -> cancelled was specified,
     permitted by the trigger, and unreachable in the product.
  3. User chose "fix both gaps" -> fixed the busy-teacher presence regression
     at the root, and made accepted -> cancelled reachable. Neither is browser-
     proven; Task 13 Step 3b exists for them.
  4. User supplied Razorpay test keys -> Task 12 built and proven against the
     live test-mode API, which immediately caught the reference_id 40-char cap
     that every mock had agreed with.
  5. Vercel env vars: found a FIFTH missing var (NEXT_PUBLIC_SITE_URL) that
     would have redirected paying students to localhost, and corrected a wrong
     verification method I had given (an unsigned curl cannot distinguish a
     configured deployment from a broken one).
  6. Rebuilt watch-sessions.mjs, which predated payments and showed none of the
     money — useless for exactly the run it exists to support.
  7. Published the Task 13 run checklist as an artifact.

THE ONLY REMAINING WORK IS THE USER'S TWO-BROWSER RUN. See the resume block in
project_state.md for the artifact link and the two facts a fresh session would
otherwise get wrong.

--- TASK 13 RUN 1, 2026-08-27: money proven, teacher navigation broken ---

The user's two-browser run reached a successful Razorpay payment. THE MONEY
PATH IS PROVEN — first Razorpay-signed webhook received and handled, two real
Rs500 charges, one completed one active, nothing stuck at paid, no refunds.

THE TEACHER WAS NEVER TAKEN INTO THE ROOM. Student alone in the call; teacher
saw "paid" then "opening your room" then nothing; reload lost the card and
returned them to Available with no route in.

  DEFECT 1 (why it failed): `matched` was set inside a setRequest updater and
  read on the next line. React evaluates an updater eagerly ONLY when the
  fiber has no pending update. `paid` queued one; `active` arrived ms later;
  its updater was deferred to render; matched stayed false; router.push never
  ran. Unreachable before M3, which had a single accepted -> active update —
  inserting `paid` created the back-to-back pair. Fixed with showingRef, a ref
  we own, and the active branch now navigates first and unconditionally.

  DEFECT 2 (why nothing recovered): the catch-up query excluded `active`, so a
  reload found nothing. Same class as M2's Critical 1 — the safety net that
  should have masked defect 1. Now includes active and pushes into the call,
  filtered through effectiveStatus so an expired row cannot cause a
  dashboard <-> call redirect loop.

LESSON, third instance in this project: neither defect was reachable by tests,
types or the build, and both needed two browsers and a real payment. The
manual gate keeps paying for itself.

--- 2026-08-27: defect 2's fix confirmed in a browser ---

After redeploy, the teacher reloaded /dashboard and was taken directly into the
paid session's room. The catch-up recovery path works, and the stranded session
from run 1 was recovered rather than lost.

STILL OWED: defect 1's fix — the LIVE navigation at the moment payment clears,
with no reload. Only a clean scenario 01 proves that one, because the bug was
specifically two realtime events landing back to back.

--- 2026-08-27: SCENARIO 01 PASSES END TO END ---

Defect 1's fix confirmed live: on a clean run the teacher was taken into the
room automatically the moment payment cleared, with no reload. Both defects
from run 1 are closed and browser-proven.

M3's riskiest path is now evidence rather than assumption: Razorpay-signed
webhook received and verified, room minted, both parties in the call, money
recorded. Checklist continues at scenario 02 (expired payment window).

--- TASK 13 RUN 2, 2026-08-27: scenarios 01-03 PASS ---

01 happy path — PASSES end to end, both defects from run 1 closed and
   browser-proven (live navigation on payment, and reload recovery).
02 expired window — PASSES. Teacher went from "In a session" back to
   "Available now" on the timer alone, no clicking.
   BUT the run surfaced a real defect, now fixed (7bc3693): every terminal
   status redirected to /teachers?didNotRespond=<teacher>, so ONE fixed
   sentence served all of them. A student whose own payment window lapsed was
   told the teacher had ignored them — false, and blaming the wrong party.
   The observed status now travels and /teachers words it per outcome, with
   payment_expired saying plainly that no money was taken (the student's first
   question). `cancelled` shows no banner at all. This also retires the
   `didNotRespond` free-text URL parameter carried as a known gap since M2 —
   arbitrary text no longer reaches the page.
03 back out of checkout — PASSES, and harder than specified: the user backed
   out THREE times and paid on the fourth. Verified in the data, not on
   screen: one payment_ref, amount_paid_paise 50000, session active, both
   parties in the room. payment_ref's write-once rule held.

Reconciliation after run 2: all invariants hold, nothing stuck at paid.
Next: scenario 04 (failed payment), then 05-07.

04 failed payment — PASSES. Chose failure@razorpay, retried inside the window,
   session started normally. Verified in the data: the declined attempt left
   NO trace in the money columns — one payment_ref, 50000 paise, no refund_ref.
   That was the assertion worth checking: a failed payment writing
   amount_paid_paise would make reconciliation report money held for a session
   that never ran.

Next: 05 (student cancels mid-payment-window) and 06 (busy teacher hidden) —
BOTH BUILT TODAY AND NEVER RUN IN A BROWSER. Then 07, the pay/cancel race,
whose refund arm has never executed against a real captured payment.

05 student cancels mid-payment-window — PASSES. Teacher went available the
   instant cancel was clicked, which was the assertion. Surfaced a separate
   real defect: the student landed back on the filtered list with every
   "Start now" greyed out, and had to walk /find again to re-pick what they
   had already picked. Fixed at the class level (d8ed6b0) rather than by
   patching one navigation: the search lived ONLY in the url, so browser back,
   a bookmark, a shared link and any future redirect all dead-ended the same
   way. /teachers now recovers the criteria from the student's own last
   session row when the url does not carry them. CONFIRMED FIXED in browser.

06 busy teacher hidden — PASSES. With a third browser engaging the teacher,
   the teacher DISAPPEARS from the first student's list entirely and the list
   shows its empty state. That is the specified behaviour, not a defect:
   parent spec §125 and M2 design §3.1 both require busy teachers be HIDDEN,
   not greyed — "every visible card is genuinely startable". The presence
   root fix built this morning is therefore browser-proven.

   PRODUCT NOTE, not a bug, worth carrying into the redesign: with a supply of
   one teacher, "hidden" reads to the student as "this product is empty". The
   rule is right; the empty state it produces is doing a lot of work it was
   never designed for. Revisit when the student dashboard / IA cycle lands —
   the honest fix is a better empty state (ask this teacher to come online,
   or notify me), not a greyed card that lies about being startable.

Remaining: 07, the pay/cancel race. Its refund arm has NEVER executed against
a real captured payment.

07 pay/cancel race — BOTH ARMS PASS. THE MANUAL GATE IS COMPLETE.

  Arm 1 (cancel loses): pressed cancel while the payment landed; the payment
  won, the student went into the call, and the row is active with 50000 paise
  and NO stray cancelled row. The compare-and-swap held — cancel matched zero
  rows and reported it could not cancel rather than racing the webhook.

  Arm 2 (refund) — proved DETERMINISTICALLY rather than by racing. The user
  could not reliably win the race, so instead: a session already `cancelled`
  still had its live Razorpay link (payment_checkout_url is stamped when Pay
  is clicked and survives the cancel). Paying that link makes the webhook
  arrive for a row that is no longer `accepted`, which is exactly the branch
  that must refund. Worth keeping as a technique — it turns the one
  money-losing race in the system into a repeatable test.

    session 97caf2c1: status cancelled, amount_paid_paise 50000,
    payment_ref plink_TUuOURvp79hfKq, refund_ref rfnd_TUuTEnxMKT6aTM

  THE IDENTIFIER TRAP IS NOW PROVEN AGAINST A REAL CAPTURED PAYMENT. refund()
  resolves the pay_ id from the plink_ id via GET /v1/payment_links/{id}. It
  had only ever run against an UNPAID link, where the payments array is empty
  — the case that proves nothing. Spec §11.1's claim that this needed no
  0006 migration is now evidence, not reasoning.

ALL SEVEN SCENARIOS PASS. Task 13's manual gate is done. Remaining work is
mechanical and controller-owned: reconcile, rebase onto origin/main, merge,
move Razorpay's webhook to the production url, re-probe production, confirm
/dev/checkout 404s there.

--- 2026-08-28: DEFERRED, then merged ---

DEFERRED BY THE USER to the next session: verifying the refund MESSAGE fix
(02a15f0). The refund itself is proven at Razorpay; what has never been seen
is the student being told. Reproduce with the payment_checkout_url trick above.
